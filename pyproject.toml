[project]
name = "general-ludd-agent"
version = "0.1.0-beta.3"
description = "General Ludd Agent — the black swan agentic coding system with Ansible runners and multi-model AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE", "THIRD_PARTY_LICENSES.md"]
authors = [{name = "General Ludd Team"}]

dependencies = [
    "fastapi>=0.1.0-beta.3",
    "starlette>=0.1.0-beta.3",
    "uvicorn-worker>=0.1.0-beta.3",
    "gunicorn>=0.1.0-beta.3",
    "httpx>=0.1.0-beta.3",
        "pydantic>=0.1.0-beta.3",
    "pydantic-settings>=0.1.0-beta.3",
    "sqlalchemy>=0.1.0-beta.3",
    "cryptography>=0.1.0-beta.3",
    "msgpack>=0.1.0-beta.3",
    "alembic>=0.1.0-beta.3",
    "psycopg[binary]>=0.1.0-beta.3",
    "aiosqlite>=0.1.0-beta.3",
    "structlog>=0.1.0-beta.3",
    "prometheus-client>=0.1.0-beta.3",
    "psutil>=0.1.0-beta.3",
    "tenacity>=0.1.0-beta.3",
    "ansible-core>=0.1.0-beta.3",
    "ansible-runner>=0.1.0-beta.3",
    "jinja2>=0.1.0-beta.3",
    "jsonschema>=4.21",
    "pyyaml>=0.1.0-beta.3",
    "hvac>=0.1.0-beta.3",
    "requests>=0.1.0-beta.3",
    "tree-sitter>=0.1.0-beta.3",
    "tree-sitter-python>=0.1.0-beta.3",
    "huggingface-hub>=0.1.0-beta.3",
    "langgraph>=0.1.0-beta.3",
    "langsmith>=0.1.0-beta.3",
    "watchdog>=0.1.0-beta.3",
    "fs>=0.1.0-beta.3",
    "rich>=0.1.0-beta.3",
    "diskcache>=0.1.0-beta.3",
    "prompt_toolkit>=0.1.0-beta.3",
    "pygments>=0.1.0-beta.3",
    "croniter>=0.1.0-beta.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=0.1.0-beta.3",
    "pytest-cov>=0.1.0-beta.3",
    "pytest-asyncio>=0.1.0-beta.3",
    "pytest-xdist>=0.1.0-beta.3",
    "pytest-timeout>=0.1.0-beta.3",
    "ruff>=0.1.0-beta.3",
    "mypy>=0.1.0-beta.3",
    "pre-commit>=0.1.0-beta.3",
    "pytest-ansible>=0.1.0-beta.3",
    "molecule>=0.1.0-beta.3",
    "ansible-lint>=0.1.0-beta.3",
    "ansible-dev-tools>=0.1.0-beta.3",
    "types-requests>=0.1.0-beta.3",
    "types-psutil>=0.1.0-beta.3",
    "aiosqlite>=0.1.0-beta.3",
    "greenlet>=0.1.0-beta.3",
    "pyinstaller>=0.1.0-beta.3",
    "bandit>=0.1.0-beta.3",
    "cyclonedx-py>=0.1.0-beta.3",
    "pip-audit>=0.1.0-beta.3",
    "pip>=0.1.0-beta.3",
]
local-inference = [
    "llama-cpp-python>=0.1.0-beta.3",
    "vllm>=0.1.0-beta.3",
]
aws = [
    "boto3>=0.1.0-beta.3",
]
gcp = [
    "google-api-python-client>=0.1.0-beta.3",
    "google-auth>=0.1.0-beta.3",
    "google-cloud-core>=0.1.0-beta.3",
]
azure = [
    "azure-identity>=0.1.0-beta.3",
    "azure-mgmt-compute>=0.1.0-beta.3",
    "azure-mgmt-authorization>=0.1.0-beta.3",
    "azure-mgmt-resource>=0.1.0-beta.3",
]
observability = [
    "opentelemetry-api>=0.1.0-beta.3",
    "opentelemetry-sdk>=0.1.0-beta.3",
    "opentelemetry-exporter-otlp-proto-grpc>=0.1.0-beta.3",
]
sandbox = [
]

[project.scripts]
gludd = "general_ludd.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = ["src/general_ludd", "LICENSE", "THIRD_PARTY_LICENSES.md", "README.md"]

[tool.hatch.build.targets.wheel]
packages = ["src/general_ludd"]

[tool.hatch.build.targets.wheel.force-include]
"THIRD_PARTY_LICENSES.md" = "general_ludd/THIRD_PARTY_LICENSES.md"

[tool.ruff]
target-version = "py311"
src = ["src", "tests"]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["general_ludd"]

[tool.mypy]
python_version = "3.11"
strict = true
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
exclude = ["scripts/", "tests/unit/test_injection_patterns.py", "tests/unit/test_sql_injection_patterns.py", "tests/unit/test_ci_push_guard.py", "abtest/"]

[[tool.mypy.overrides]]
module = ["pygments", "pygments.*", "ansible.*", "ansible_collections.*", "hvac.*", "psycopg.*", "yaml.*", "tree_sitter.*", "tree_sitter_python.*", "huggingface_hub.*", "diskcache.*", "watchdog.*", "croniter.*", "jsonschema", "googleapiclient.*", "google.*", "azure.*", "ansible_runner", "xmlsec", "lxml", "lxml.etree", "pyVim.*", "pyVmomi", "openai"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "scapy.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "general_ludd.ansible.core_runner"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "general_ludd.db.models"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "general_ludd.observability.metrics_exporter"
disable_error_code = ["type-arg"]

[[tool.mypy.overrides]]
module = ["tests", "tests.*"]
disallow_untyped_defs = false
disallow_untyped_calls = false
disallow_incomplete_defs = false
check_untyped_defs = false
disable_error_code = ["attr-defined", "no-untyped-def", "no-untyped-call"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short --ignore=tests/fixtures -m \"not hook_live\""
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
timeout = 180
timeout_method = "signal"
markers = [
    "xdist_group: serialize tests that share a fixed resource (e.g. port 8000 or the daemon PID file) under pytest-xdist",
    "requires_slurm: skip unless SLURM_AVAILABLE=1 is set in env or sbatch is on PATH",
    "requires_postgres: skip unless POSTGRES_AVAILABLE=1 is set in env or port 5432 is open",
    "hook_live: exercises a real opencode CLI against the .opencode/plugin/*.ts hooks; excluded from the default run (`-m \"not hook_live\"` in addopts) — run via `make test-hooks-live` or `-m hook_live`",
]
filterwarnings = [
    "ignore:pkg_resources is deprecated as an API:UserWarning:fs",
    "ignore:Deprecated call to `pkg_resources\\.declare_namespace\\('fs.*:DeprecationWarning:fs",
    "ignore:Deprecated call to `pkg_resources\\.declare_namespace\\('fs.*:DeprecationWarning:pkg_resources",
]

[tool.coverage.run]
source = ["general_ludd"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 85
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]

[dependency-groups]
dev = [
    "pytest>=0.1.0-beta.3",
    "pytest-cov>=0.1.0-beta.3",
    "pytest-asyncio>=0.1.0-beta.3",
    "pytest-xdist>=0.1.0-beta.3",
    "pytest-timeout>=0.1.0-beta.3",
    "ruff>=0.1.0-beta.3",
    "mypy>=0.1.0-beta.3",
    "pre-commit>=0.1.0-beta.3",
    "detect-secrets>=0.1.0-beta.3",
    "types-requests>=0.1.0-beta.3",
    "types-psutil>=0.1.0-beta.3",
    "aiosqlite>=0.1.0-beta.3",
    "greenlet>=0.1.0-beta.3",
    "pyinstaller>=0.1.0-beta.3",
    "bandit>=0.1.0-beta.3",
    "cyclonedx-py>=0.1.0-beta.3",
    "pip-audit>=0.1.0-beta.3",
    "deptry>=0.1.0-beta.3",
    "molecule>=0.1.0-beta.3",
    "ansible-lint>=0.1.0-beta.3",
]
