---
# evaluate_model role tasks — ai_ml service.

- name: Load evaluate_model defaults
  ansible.builtin.include_vars:
    file: "{{ role_path }}/defaults/main.yml"
    name: _evaluate_model_defaults

- name: Reference evaluate_model service API
  ansible.builtin.debug:
    msg: "evaluate_model routes via the ai_ml service API (/api/ai_ml/evaluate_model)."
