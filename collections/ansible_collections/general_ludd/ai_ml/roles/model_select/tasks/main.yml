---
# model_select role tasks — ai_ml service.

- name: Load model_select defaults
  ansible.builtin.include_vars:
    file: "{{ role_path }}/defaults/main.yml"
    name: _model_select_defaults

- name: Reference model_select service API
  ansible.builtin.debug:
    msg: "model_select routes via the ai_ml service API (/api/ai_ml/model_select)."
