---
galaxy_info:
  role_name: standup_report
  author: Agentic Harness Agent
  description: >
    Yesterday/today/blockers standup from gludd_facts (work/history) and
    gludd_message receive (blockers). The mock /api/messages seeds a blocker
    message (topic=standup) which this role surfaces in blockers[].
    REPORT-ONLY: never mutates the repo.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
