# tool_dispatch

Dispatch a tool call through the daemon tool router.

## FQCN

`general_ludd.agent.tool_dispatch`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.tool_dispatch
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
