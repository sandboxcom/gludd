# budget_guard

Check remaining run budget; defer/abort when below threshold.

## FQCN

`general_ludd.agent.budget_guard`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.budget_guard
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
