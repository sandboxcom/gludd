# lint_and_check

Run lint and typecheck commands and write a combined pass/fail artifact.
Used by the `dependency_update` playbook.

Invoked as `general_ludd.agent.lint_and_check` (collection FQCN).
