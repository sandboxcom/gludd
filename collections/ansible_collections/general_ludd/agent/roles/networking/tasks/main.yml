---
# Compatibility shim. The implementation moved to the dedicated networking
# collection; keep the original agent role callable without duplicating tasks.

- name: Delegate networking operations to the canonical collection role
  ansible.builtin.include_role:
    name: general_ludd.networking.networking
