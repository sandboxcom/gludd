# run_tests

Run a project test command and write the result (rc, stdout tail, status)
to a JSON artifact. Thin wrapper used by the `molecule_test` playbook.

Invoked as `general_ludd.agent.run_tests` (collection FQCN).
