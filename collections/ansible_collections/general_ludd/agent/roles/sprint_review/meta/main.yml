---
galaxy_info:
  role_name: sprint_review
  author: Agentic Harness Agent
  description: >
    Completed-work demo summary from gludd_facts.history + gludd_facts.traces.
    Produces completed[] (from history total_runs/success), highlights[] (from
    recent execution traces), and demo_notes (from by_phase trace aggregate).
    REPORT-ONLY: never mutates the repo.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
