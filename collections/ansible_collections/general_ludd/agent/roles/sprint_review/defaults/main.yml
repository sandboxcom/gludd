---
# sprint_review role defaults — REPORT-ONLY

# Daemon connectivity
daemon_url: "http://localhost:8000"
psk: ""

# Artifact output
artifact_dir: "/tmp/gludd-sprint-review"

# Sprint name for the review report
sprint_name: "Sprint-1"

# Destructive ops guard — report-only by default
enable_git_push: false
