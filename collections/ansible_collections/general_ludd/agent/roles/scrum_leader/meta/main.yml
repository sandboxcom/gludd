---
galaxy_info:
  role_name: scrum_leader
  author: Agentic Harness Agent
  description: >
    Pure composer role over agile planning ceremony roles (backlog_groom,
    sprint_plan, standup_report, retrospective). Includes each ceremony role
    in sequence, then slurps and consolidates sub-artifacts into a combined
    sprint index. No new decision logic — wires includes. NEVER mutates the repo.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
