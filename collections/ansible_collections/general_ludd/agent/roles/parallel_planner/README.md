# parallel_planner

Compute a concurrency-safe execution plan via the scheduler (plan-only).

## FQCN

`general_ludd.agent.parallel_planner`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.parallel_planner
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
