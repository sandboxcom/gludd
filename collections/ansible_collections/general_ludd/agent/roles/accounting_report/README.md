# accounting_report

Fetch per-project accounting; write cost/time/LoC/role/todo report artifact (report-only).

## FQCN

`general_ludd.agent.accounting_report`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.accounting_report
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
