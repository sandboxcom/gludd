# ai_parallel_dispatch

Dispatch a batch of AI sub-tasks concurrently via the daemon.

## FQCN

`general_ludd.agent.ai_parallel_dispatch`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.ai_parallel_dispatch
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
