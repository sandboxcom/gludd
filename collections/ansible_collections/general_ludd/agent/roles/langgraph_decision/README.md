# langgraph_decision

Drive a LangGraph decision flow and surface the chosen branch.

## FQCN

`general_ludd.agent.langgraph_decision`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.langgraph_decision
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
