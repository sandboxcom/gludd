---
galaxy_info:
  role_name: langgraph_decision
  author: Agentic Harness Agent
  description: >
    Model-backed decision role: run a langgraph workflow over a prompt, judge
    the result with a langgraph decision node, optionally revise once on a
    'revise' verdict, surface the decision back to gludd, and write an artifact
    for human inspection. Wraps the model work in block/rescue/always.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
