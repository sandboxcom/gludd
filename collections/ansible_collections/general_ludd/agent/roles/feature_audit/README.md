# feature_audit

Audit a feature for completeness across CLI/daemon/TUI/interfaces.

## FQCN

`general_ludd.agent.feature_audit`

## Example

```yaml
- hosts: localhost
  roles:
    - role: general_ludd.agent.feature_audit
```

## Inputs

See `defaults/main.yml` for the full variable list with defaults.
