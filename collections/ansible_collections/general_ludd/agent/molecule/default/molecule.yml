---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: localhost
provisioner:
  name: ansible
  config_options:
    defaults:
      gathering: explicit
      callbacks_enabled: ansible.posix.profile_tasks
  playbooks:
    converge: converge.yml
verifier:
  name: ansible
