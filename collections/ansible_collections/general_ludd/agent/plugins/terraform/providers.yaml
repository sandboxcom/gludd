# Declared terraform provider dependencies for this collection.
# Mirrors requirements.yml for ansible roles. The importer cross-checks
# every entry against infra/terraform/policies/data.json provider_trust_list.
providers:
  - name: vsphere
    source: hashicorp/vsphere
    version: "~> 2.8"
