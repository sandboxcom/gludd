---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include android_security"
      ansible.builtin.include_role:
        name: "general_ludd.os_expert.android_security"
      vars:
        enable_git_push: false
