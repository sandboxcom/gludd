# windows_security

Audit Windows security posture: Defender antivirus configuration
(Get-MpPreference), Windows Firewall rules (Get-FirewallRule), audit
policy settings (auditpol), and security configuration analysis
(secedit). Report-only, producing a JSON findings artifact.
