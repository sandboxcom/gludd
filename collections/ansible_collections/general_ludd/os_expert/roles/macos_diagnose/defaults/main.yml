---
# macos_diagnose role

# Daemon connectivity
daemon_url: "http://localhost:8000"
psk: ""

# Artifact output
artifact_dir: "/tmp/gludd-macos-diagnose"

# Diagnostic gather targets
gather_unified_log: true
gather_spdiagnose: true
gather_launchctl: true
gather_nvram: true
gather_pmset: true

# SAFE-BY-DEFAULT: report only
enable_git_push: false
