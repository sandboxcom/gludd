---
- name: Converge
  hosts: localhost
  gather_facts: true
  tasks:
    - name: "Include macos_diagnose"
      ansible.builtin.include_role:
        name: "macos_diagnose"
      vars:
        enable_git_push: false
