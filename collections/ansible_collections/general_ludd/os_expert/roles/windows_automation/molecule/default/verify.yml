---
- name: Verify windows_automation
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Role included successfully
      ansible.builtin.assert:
        that: true
        success_msg: "windows_automation role verified (tasks skipped on non-Windows host)"
