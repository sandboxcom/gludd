# windows_automation

Automate Windows system administration: PowerShell Remoting (Enable-PSRemoting)
configuration, Desired State Configuration (Start-DscConfiguration) application,
scheduled task (schtasks) creation and lifecycle, and unattended software
installation. All destructive operations gated behind explicit enable flags.
