# linux_automation

Automate Linux system maintenance: systemd timer unit creation and lifecycle,
unattended-upgrades scheduling and configuration, cron job management, and
logrotate policy deployment. All destructive operations gated behind
explicit enable flags.
