---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include linux_diagnose"
      ansible.builtin.include_role:
        name: "linux_diagnose"
      vars:
        enable_git_push: false
