# macos_security

Audit macOS security posture: System Integrity Protection (csrutil),
Gatekeeper assessment policy (spctl), XProtect malware definitions,
TCC privacy database (tccutil), and plist-based policy configurations.
Report-only, with findings written to a JSON artifact.
