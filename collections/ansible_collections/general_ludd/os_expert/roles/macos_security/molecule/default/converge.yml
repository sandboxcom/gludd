---
- name: Converge
  hosts: localhost
  gather_facts: true
  tasks:
    - name: "Include macos_security"
      ansible.builtin.include_role:
        name: "macos_security"
      vars:
        enable_git_push: false
