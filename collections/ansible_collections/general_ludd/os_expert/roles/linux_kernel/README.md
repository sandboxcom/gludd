# linux_kernel

Manage Linux kernel subsystems: module loading (modprobe), eBPF program
attachment, namespace isolation (network/pid/mount), cgroup resource limits,
and sysctl kernel parameter tuning. Destructive operations gated behind
explicit enable flags.
