---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include linux_kernel"
      ansible.builtin.include_role:
        name: "general_ludd.os_expert.linux_kernel"
      vars:
        enable_git_push: false
