# ios_diagnose

Gather iOS diagnostic data via libimobiledevice tools: device information
(ideviceinfo), system log stream (idevicesyslog), diagnostics reports
(idevicediagnostics), and unified log (oslog). Requires a USB-connected
iOS device with libimobiledevice installed.
