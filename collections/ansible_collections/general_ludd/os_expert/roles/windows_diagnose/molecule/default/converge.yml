---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include windows_diagnose"
      ansible.builtin.include_role:
        name: "general_ludd.os_expert.windows_diagnose"
