# windows_diagnose

Query Windows diagnostic data via PowerShell: WMI/CIM system information
(Get-WmiObject), Windows Event Log entries (Get-EventLog), registry key
values (reg query), and service controller state (sc query).
