---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include ios_security"
      ansible.builtin.include_role:
        name: "general_ludd.os_expert.ios_security"
      vars:
        enable_git_push: false
