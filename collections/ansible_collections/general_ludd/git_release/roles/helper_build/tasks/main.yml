---
# helper_build role tasks — git_release service.

- name: Load helper_build defaults
  ansible.builtin.include_vars:
    file: "{{ role_path }}/defaults/main.yml"
    name: _helper_build_defaults

- name: Reference helper_build service API
  ansible.builtin.debug:
    msg: "helper_build routes via the git_release service API (/api/git_release/helper_build)."
