---
galaxy_info:
  role_name: saml_processor
  namespace: general_ludd
  author: Agentic Harness Agent
  description: >
    SAML 2.0 assertion processor — parse AuthnRequest, Response, and
    Assertion documents. Extract attributes, NameID, Audience, and
    conditions (NotBefore/NotOnOrAfter). Detect XMLDSig signatures.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
