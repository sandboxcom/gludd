---
galaxy_info:
  role_name: http_server
  author: Agentic Harness Agent
  description: >
    Web server setup and configuration — installs and configures nginx or
    Apache, manages virtual hosts, document roots, modules, proxy pass,
    rate limiting, buffering, and compression. Includes a Python config
    parser/validator and virtual host template generator.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
