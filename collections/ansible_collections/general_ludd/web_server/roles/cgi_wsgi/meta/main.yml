---
galaxy_info:
  role_name: cgi_wsgi
  author: Agentic Harness Agent
  description: >
    CGI/FastCGI/SCGI/PSGI/WSGI/ASGI gateway configuration — configures gateway
    interfaces, sets up application server sockets, manages process pools,
    and generates WSGI/ASGI configs. Includes a Python config generator and
    socket path validator.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: [all]
    - name: Ubuntu
      versions: [all]
    - name: MacOSX
      versions: [all]
dependencies: []
