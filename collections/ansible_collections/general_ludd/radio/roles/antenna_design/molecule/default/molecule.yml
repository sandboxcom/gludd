---
driver:
  name: delegated
  lint: |
    set -e
    yamllint .
platforms:
  - name: localhost
provisioner:
  name: ansible
  config_options:
    defaults:
      gathering: explicit
verifier:
  name: ansible
