---
galaxy_info:
  role_name: sdr_capture
  author: Gludd Agent
  description: >
    Capture IQ samples from SDR hardware — set frequency, gain, sample rate,
    and write raw samples to disk via rtl_sdr / SoapySDR.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: ["8", "9"]
    - name: Ubuntu
      versions: [focal, jammy, noble]
    - name: MacOSX
      versions: [all]
  galaxy_tags: [sdr, capture, iq, rtl_sdr, soapysdr]
dependencies: []
