---
# quantum_workflow role tasks — chemistry service.

- name: Load quantum_workflow defaults
  ansible.builtin.include_vars:
    file: "{{ role_path }}/defaults/main.yml"
    name: _quantum_workflow_defaults

- name: Reference quantum_workflow service API
  ansible.builtin.debug:
    msg: "quantum_workflow routes via the chemistry service API (/api/chemistry/quantum_workflow)."
