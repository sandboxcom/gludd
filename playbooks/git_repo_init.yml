---
- name: Initialize git repository for agentic harness
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    repo_path: "{{ repo_path | default('.') }}"
    user_email: "{{ user_email | default('agent@harness.local') }}"
    user_name: "{{ user_name | default('Agentic Harness Agent') }}"
    artifact_dir: "{{ artifact_dir | default('/tmp/harness-git-init') }}"

  tasks:
    - name: Create artifact directory
      ansible.builtin.file:
        path: "{{ artifact_dir }}"
        state: directory
        mode: "0755"

    # noqa command-instead-of-module: ansible.builtin.git cannot `git init` a
    # fresh empty repository, so command is the correct module here.
    - name: Initialize git repository
      ansible.builtin.command: # noqa: command-instead-of-module
        cmd: git init
        chdir: "{{ repo_path }}"
      register: init_result
      changed_when: "'Initialized' in init_result.stdout"

    # noqa command-instead-of-module: the git module has no `git config`
    # capability; setting user.email requires command.
    - name: Configure git user email
      ansible.builtin.command: # noqa: command-instead-of-module
        cmd: git config user.email "{{ user_email }}"
        chdir: "{{ repo_path }}"
      changed_when: false

    # noqa command-instead-of-module: the git module has no `git config`
    # capability; setting user.name requires command.
    - name: Configure git user name
      ansible.builtin.command: # noqa: command-instead-of-module
        cmd: git config user.name "{{ user_name }}"
        chdir: "{{ repo_path }}"
      changed_when: false

    - name: Write init result artifact
      ansible.builtin.copy:
        content: |
          {
            "repo_path": "{{ repo_path }}",
            "playbook": "git_repo_init.yml",
            "status": "success",
            "stdout": "{{ init_result.stdout | default('') }}"
          }
        dest: "{{ artifact_dir }}/git_init_result.json"
        mode: "0644"
