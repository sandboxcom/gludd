---
- name: Attest and launch a provider-neutral model worker generation
  hosts: gludd_model_workers
  gather_facts: false
  serial: 1
  any_errors_fatal: true
  roles:
    - role: general_ludd.agent.attest_model_worker
    - role: general_ludd.agent.model_worker
