---
- name: "Travel collection verify"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Verify travel collection integration"
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Travel collection verified"
