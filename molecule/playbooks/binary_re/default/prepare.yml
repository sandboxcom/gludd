---
- name: Prepare (no-op)
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No preparation needed
      ansible.builtin.debug:
        msg: "No preparation needed for binary_re molecule scenario"
