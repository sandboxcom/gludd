---
- name: Converge — run gha_billing role with mock data
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Invoke gha_billing
      ansible.builtin.include_role:
        name: general_ludd.agent.gha_billing
      vars:
        gha_billing_token_source: "mock"
