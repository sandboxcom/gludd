---
- name: Prepare (no-op)
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No preparation needed
      ansible.builtin.debug:
        msg: "No preparation needed for runtime_validate molecule scenario"
