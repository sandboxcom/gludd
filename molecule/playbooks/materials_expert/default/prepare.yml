---
# prepare: no external fixtures needed; material_select creates its own output dir.
- name: Prepare - noop for materials_expert
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Sentinel (no fixtures required)
      ansible.builtin.debug:
        msg: "materials_expert prepare: material_select is self-contained."
