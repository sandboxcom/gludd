---
driver:
  name: default
platforms: []
provisioner:
  name: ansible
  env:
    # Resolve the in-repo collection (general_ludd.agent.*).
    ANSIBLE_COLLECTIONS_PATH: "${MOLECULE_PROJECT_DIRECTORY}/collections"
  playbooks:
    prepare: default/prepare.yml
    converge: default/converge.yml
    verify: default/verify.yml
verifier:
  name: ansible
