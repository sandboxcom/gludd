# Foundation Regression Check

Read-only verification that the `[completed]` foundation tasks (#12–#34 + #41/#42/#44/#45/#46/#48) are STILL grounded after the large recent wave (which touched scoring / security-auth / budget / db / skills). Every verdict below is grounded in code/tests actually read via Read + `make grep`. No pytest was run (a gate was in flight); verdicts are "green-by-inspection" (real assertions present, no xfail/skip masking the foundation).

Date: 2026-06-16 · Method: Read + `make grep` only · Scope: read-only, no files changed except this doc.

## Summary

All eight focus items verified STILL-DONE. No foundation test was found deleted, xfail'd, or skipped by the wave. The only ratchet xfails (`config/ratchet.yml`, 12 entries) are pre-existing flaky/environment cases (watchdog FSEvents, PTY/TUI, SAST, hvac race) — none touch #34/#42/#44/#45/#46/#48 or the other foundation tasks. The two red-team suites that previously pinned the foundation bugs as `xfail(strict=True)` (eventloop, git-worktree) have had those markers REMOVED and now assert the fixed behavior (would turn red on regression) — verified from the file headers, not just inferred.

## Verdict table

| Task | Verdict | Evidence (file:line — what was read) |
|------|---------|--------------------------------------|
| #34 live pricing / billing intelligence (models/pricing, gateway) | STILL-DONE | `src/general_ludd/pricing_intel/sources.py:78-177` `OpenRouterSource` is a genuine LIVE fetch (public `/api/v1/models`, fail-soft → `[]` on error); `all_sources()` registry at `:900-912`; consumed by `pricing_intel/catalog.py:31,66` (`PricingCatalog(sources=… or all_sources())`). Billing intelligence wired into the gateway: `src/general_ludd/models/gateway.py:60-63` per-token cost + `run_budget_usd`; `:308-322` computes `cost` and calls `_budget_guard.record_spend(cost)`; `:27-40` `_coerce_token_count` hardens untrusted provider token counts (rejects bool/negative so a malicious count can't credit the budget). Intact and wired. |
| #42 saturation controller (keep-N-busy) | STILL-DONE | `src/general_ludd/controllers/saturation.py` — pure, stateless `SaturationController`: `plan_backfill`/`plan_backfill_by_source` compute `backfill = max(0, target - running)` bounded by backlog + per-source headroom (`:148-180`); `utilization` clamps `[0,1]` (`:83-100`). Composed by the pipeline: `src/general_ludd/pipeline/lanes.py:27,107,186` (`DispatchLane` holds a `SaturationController` and calls `plan_backfill` for floor backfill). Tests: `tests/unit/test_saturation_controller.py` — 17 tests incl. `test_backfills_exactly_to_target`, `test_zero_idle_when_backlog_available`, `test_pulls_zero_when_running_equals_target`, `test_never_exceeds_single_source_cap`, `test_full_saturation_then_backfill_zero`. No skips/xfails. |
| #44 unified capability policy (capability_policy ↔ gludd_db) | STILL-DONE | Lattice: `src/general_ludd/security/capability_lattice.py` (default-DENY). Wiring test `tests/security/test_gludd_db_capability_wiring.py` statically walks every `gludd_db` call in roles + molecule scenarios, fails-closed if zero discovered (`:218-223`), then parametrizes `test_every_gludd_db_op_is_granted_to_its_role` asserting the resolved role's REAL `policy.check_db_op(op)` grants it (`:251-262`). `tests/security/test_capability_policy.py` (~40 tests) asserts `gludd_db` imports the guard (`test_module_imports_the_guard`), declares the role param, calls `check_db_op`, and the check precedes the daemon call. No stubs, no skips. |
| #45 reload fix | STILL-DONE | Unit: `tests/unit/test_w3_12_reload.py` — 4 real tests (`test_models_reloaded_*`, `test_reload_reports_no_success_for_noop_paths`, `test_reload_manager_unknown_reload_id_returns_failed_status`); only an import-guard `pytest.skip("ReloadManager not importable")` (`:76`), not a foundation skip. Red-team: `tests/security/test_reload_redteam.py` — 6 real tests (sentinel-forgery / forged-OK / no-health-check-swap / rollback-not-verified). Hot reloader code present at `src/general_ludd/reload/hot_reloader.py` (routing-config apply to gateway, `:428-477`). |
| #46 red-team fixes | STILL-DONE | Broad live red-team coverage present: `tests/security/test_eventloop_redteam.py`, `test_git_worktree_redteam.py`, `test_gateway_redteam.py`, `test_gateway_concurrency_redteam.py`, `test_daemon_auth_redteam.py`, `test_router_auth_redteam.py`, `test_db_redteam.py`, `test_filestore_redteam.py`, `test_secrets_redteam.py`, `test_mcp_redteam.py`, `test_ansible_redteam.py`, `test_skills_ssti_injection_redteam.py`. Critically, `test_eventloop_redteam.py:1-14` header states the bug-proving tests' `xfail(strict=True)` markers were REMOVED because the concurrency fixes landed — they now assert FIXED behavior and turn red on regression. Same pattern noted in `test_git_worktree_redteam.py:17`. So the wave did NOT silently re-xfail these. |
| #48 execution-engine fixes | STILL-DONE | `tests/unit/test_execution_engine_fixes.py:1-28` covers three confirmed fixes: (1) `GitAutomation._run_git` passes `timeout=` + non-interactive env (`_GIT_TIMEOUT_SECONDS` imported from `git_automation/repo.py`); (2) `ToolCallLoop` bounds each MCP call with `asyncio.wait_for`→tool-error; (3) `ExecutionEngine` refuses model FILE:/diff paths escaping the workspace (abs / `../`). Real imports of `ExecutionEngine`, `ToolCallLoop`, `GitAutomation`. No xfail/skip. |
| #24 auditor / analyst roles | STILL-DONE | `make collection-roles` lists the auditor family: `cost_optimization_auditor`, `dead_code_auditor`, `dry_code_auditor`, `enhancement_auditor`, `feature_gap_auditor`, `feature_audit`, `report_audit`, `audit_dependencies`, `audit_security`; and the analyst family: `log_analyst`, `qa_analyst`, `soc_analyst`, `ui_ux_analyst`. All present as real Ansible roles under the collection. |
| #29 feature DB | STILL-DONE | `src/general_ludd/db/models.py:338-357` `FeatureModel` (`__tablename__ = "features"`, id default `_gen_feature_id`). `src/general_ludd/db/repository.py:961-1069` `FeatureRepository` (upsert keyed by unique name, `get_by_id`, `update`). `src/general_ludd/db/feature_seed.py` seeds real feature rows with evidence refs; `/api/features` endpoints referenced in the seed. Evidence tests named in seed: `tests/unit/test_feature_verifier.py`, `tests/unit/test_feature_repo.py`. |
| #30 CI-error regression tests | STILL-DONE | `tests/unit/test_ci_regression_guards.py` — repo-config guards for real CI failures hit in-session: `build.yml` present + parses with `jobs` (`:32-37`), Windows cp1252 pyinstaller spec guard (`:58-62`), `gate`/`windows`/`release` jobs present (`:82,112,194`), 40-min gate-budget guard (`:93`), molecule scenarios present (`:154-170`), release fires only on tags / after all platform artifacts (`:200-213`). Comprehensive and current. |
| #33 conflict scanner | STILL-DONE | `scripts/scan_conflicts.py` (flags real `<<<<<<<`/`=======`/`>>>>>>>` hunks; markdown/rst false-positive handling; skips `conflict_fixtures`). `tests/unit/test_conflict_scanner.py` — 16 tests (ours/theirs/merge-base markers, full block flags all four, bare-equals not flagged, binary skipped, fixture dir skipped, main returns 0/nonzero). `make scan-conflicts` target present. |

## Notes / non-issues

- The #34 "live pricing" line covers TWO distinct subsystems and both are intact: (a) `pricing_intel/` provider-pricing catalog (OpenRouter LIVE, others STATIC-with-source-URL + documented `TODO(integration)`), and (b) `models/gateway.py` per-call cost accounting + budget guard. Neither was regressed by the wave's budget/scoring changes — `_coerce_token_count` and `record_spend(cost)` are still the wired path.
- Working-tree modifications observed at start (`config/ratchet.yml`, `tests/integration/test_daemon_lifespan.py`, `tests/unit/test_guardrails.py`, `tests/unit/test_tui_extracted_builders.py`) do NOT add any foundation task to the ratchet — the read `ratchet.yml` contains only flaky/environment/pre-existing entries.

## Limitation

Verdicts are by inspection only (a gate was running; pytest was deliberately not invoked per the audit constraints). "Green-by-inspection" means: the implementation code is present and wired, the named tests exist as real assertions, and no xfail/skip/deletion was found masking them. It does NOT re-run the suite. If a runtime regression slipped past static reading, only an actual `make gate` / `make test` would surface it.
