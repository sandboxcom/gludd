# Symbiotic Agent Integration

Superseded by [SYMBIOTIC_AGENT_INTEGRATION.md](SYMBIOTIC_AGENT_INTEGRATION.md) — this file was a reflowed duplicate that also corrupted several `<think>`/`<tool_call>` tag references (mangled to "question"/"function"); do not use it as a source.
