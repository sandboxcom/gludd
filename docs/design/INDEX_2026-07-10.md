# Design Corpus Index — 2026-07-10

Master navigation + implementation-readiness map for the design work produced/updated
in the 2026-07-10 session. Companion to (not a replacement for) the older
[`docs/design/index.md`](index.md), which indexes the pre-existing design corpus.

**Survey method:** every doc below was read at its header/status section (not always
cover-to-cover); `HARDENING_BACKLOG_2026-07-10.md` and `WAVE_C_DESIGNS_2026-07-10.md`
were read in full for item enumeration. Line numbers cited in the source docs are
current-tree-at-authoring-time and drift — re-Read before editing, per every doc's own
disclaimer.

**Corpus size:** `docs/design/` contains **53 files** (49 git-tracked + 4 untracked-new-
this-session: `ANSIBLE_COLLECTION_TRUST.md`, `BINARY_BUNDLING.md`,
`PIPELINE_INTERACTION_ROLES.md`, `TIME_TIMERS_SCOPED_NOTIFICATIONS.md`). This index
covers the **17 docs produced/touched in the 2026-07-10 session** (9 feature specs + 8
security/process docs); the other 36 pre-2026-07-10 docs are listed by name only in
§6 (out of scope for this pass — not read/summarized here).

---

## 1. Feature specs (user-requested this session)

| Doc | Summary | Status | Key new deps | Primary files touched |
|---|---|---|---|---|
| [LOGGING_CONFIGURATION.md](LOGGING_CONFIGURATION.md) | Central pluggable logging config: per-sink format (JSON/syslog/plain), per-agent/task log files, per-component verbosity, allowlisted dotted-path handlers | design-complete, not implemented | none (extends stdlib `logging`/`dictConfig`) | `logging/project_log.py`, `observability/metrics_exporter.py`, `daemon.py`, `worker/gunicorn_conf.py`, `config/user_config.py`, `ansible/core_runner.py`, `routers/todos.py` |
| [BROWSER_AUTOMATION.md](BROWSER_AUTOMATION.md) | Human-like browser automation (forms/captcha/headers/TLS/iframes) via high-level intent verbs, no raw CSS/DOM | design-complete, not implemented | **playwright>=1.44** (sync API in `asyncio.to_thread`) | new `browser/` package, 5 new `gludd_browser_*` Ansible modules, new `roles/browse_web/`, `pyproject.toml` |
| [TIME_TIMERS_SCOPED_NOTIFICATIONS.md](TIME_TIMERS_SCOPED_NOTIFICATIONS.md) | NL time resolver + Timer entity (reuses cron scheduler) + scope-routing layer over EventBus/HookSystem/AgentMessageRepository | design-complete, not implemented | NL-datetime parser not finalized in header (candidate, unconfirmed) | `event_loop/scheduler.py`, `db/models.py` (TodoModel), `events/bus.py`, `events/hooks.py`, `db/repository.py` (AgentMessageRepository) |
| [BINARY_BUNDLING.md](BINARY_BUNDLING.md) | Unify `BinaryBootstrapper` (bundle/download) with `BinaryPathResolver` (currently PATH-only) so tofu/terraform/openbao/osquery/ripgrep/node/git resolve bundle-first with empty PATH; flags a dead ripgrep-sha256 placeholder bug | design-complete, not implemented | none | `config/binary_paths.py`, `filestore/bootstrap.py`, `infra/deployment.py`, `collections/importer.py`, `connectors/osquery.py`, `mcp/catalog.py`, `mcp/transport.py`, `Makefile` |
| [PIPELINE_INTERACTION_ROLES.md](PIPELINE_INTERACTION_ROLES.md) | New capability: agent DRIVES external CI/CD (trigger/cancel/approve/rerun) across Jenkins/GitLab/GHA/Tekton/Concourse/Drone/Woodpecker — distinct from `ci_pipeline_medic` (self-CI-fixing) | design-complete, not implemented | none new named (net-new connector adapters for Tekton/Concourse/Drone/Woodpecker) | `connectors/registry.py`, new `connectors/*` pipeline providers, `security/permissions.py`, new `gludd_pipeline_*` Ansible modules |
| [CI_PIPELINE_MEDIC_ROLE.md](CI_PIPELINE_MEDIC_ROLE.md) *(modified, uncommitted)* | New `ci_pipeline_medic` role: CI-failure taxonomy/playbook to autonomously turn red GHA green; needs prereq fix so the role executor honors role prompt/model | turnkey / ground-truth-verified this session | none | `daemon.py` (`_gateway_executor`, D-CIM-0 prereq), role registry/`AgentConfig`, new `collections/.../roles/ci_pipeline_medic/` |
| [DAST_INTEGRATION_SLICE.md](DAST_INTEGRATION_SLICE.md) | Wire a real `dast` check into `project_runner` (currently docstring-only placeholder), allowlist-first + metadata-hard-deny target resolution | design-complete, not implemented | scan tool not yet chosen (nuclei/zap not confirmed in header) | new `project_runner/dast_target.py`, `project_runner/profile.py`, `project_runner/findings.py`, `quality/project_gate.py` |
| [COMPUTE_DEPLOY_AND_SATURATE.md](COMPUTE_DEPLOY_AND_SATURATE.md) | Wire Terraform-deploy → serve → discover → register → saturate GPU compute end-to-end (D-DEPLOY + D-SATURATE), hardened SSRF trusted-origin allowance | "two ends BUILT, middle wiring is the gap" — ready-for-implementation | none | `infra/deployment.py`, `infra/local_inference.py`, `models/gateway.py`, `routers/compute.py`, new `infra/endpoint_registrar.py`, `infra/terraform.py` |
| [PARALLEL_IMPLEMENTATION_AND_PARITY.md](PARALLEL_IMPLEMENTATION_AND_PARITY.md) | Part 1: parallel/single-writer landing strategy for all Wave-C/D designs. Part 2: Wave-P competitive-parity items vs opencode/aider/goose (keystone: generic toolchain runner) | process/strategy doc, no per-item build status | none (process doc) | cross-cutting only — see §4 |

---

## 2. Security fix designs

| Doc | Summary | Status |
|---|---|---|
| [WAVE_C_DESIGNS_2026-07-10.md](WAVE_C_DESIGNS_2026-07-10.md) | Turnkey line-anchored fixes vs `master` HEAD `1c6afab0`. Doc's own title says "11 turnkey" but it actually contains **15 H2 sections** (several bundle 2+ sub-findings) — see enumeration below. | design-complete, not implemented; land after v0.1.0-beta.3 cut |
| [WAVE_C_ADDENDUM_2026-07-10.md](WAVE_C_ADDENDUM_2026-07-10.md) *(modified, uncommitted)* | Round-2/3/4 corrections: 3 items **verified ALREADY-FIXED** (do not rework) + 6 new STILL-OPEN findings | corrections/addendum |
| [WAVE_D_DESIGNS_2026-07-10.md](WAVE_D_DESIGNS_2026-07-10.md) | *(not in original brief — found in survey)* Feature-side (not security) Wave D companion: §D2–D8 + §SPD-1, mapped to spec IDs D2/D3/D12-slice1/D5/D9+D10/D11/D4/SPD-1. D2 wires `run_project_gate()` (zero callers today) into the completion-transition path; D3 makes self-improve apply path project-neutral | design-complete, not implemented |
| [ANSIBLE_COLLECTION_TRUST.md](ANSIBLE_COLLECTION_TRUST.md) *(untracked, new)* | **CRITICAL, confirmed.** Project-tier `.gludd/collections/` shadows bundled `general_ludd.*` roles by FQCN precedence (project-first beats bundled) → full in-process RCE via a cloned/onboarded repo shipping a same-named role; secondary SSTI vector via non-sandboxed Templar on the shadowed role's templates | design-complete, not implemented; must land as ONE batch (paths.py + daemon.py + docs + cli_project_init are tightly coupled) |
| [ISSUE_SOURCES_SSRF_AND_BOUNDED_HTTP.md](ISSUE_SOURCES_SSRF_AND_BOUNDED_HTTP.md) | Part A (land first): 5 issue-source adapters (linear, trello, azure_boards, asana, servicenow) have weaker local SSRF guards that let `metadata.google.internal`/`instance-data` through via a swallowed-ValueError bypass — fix delegates to canonical `security/ssrf.py::host_is_blocked` (jira/clickup/monday/bitbucket_issues/gitlab_issues already correct, used as reference). Part B (independent, lands after): shared bounded-HTTP/zip-bomb guard module | Part A design-complete; Part B not fully scoped |
| [HARDENING_BACKLOG_2026-07-10.md](HARDENING_BACKLOG_2026-07-10.md) *(modified, uncommitted)* | Line-anchored designs for OPEN hardening items across **7 rounds** (not 6 as originally briefed — a "Round-7 additions" section exists). **33 items enumerated** (32 H-prefixed + 1 non-H CRITICAL, C22-SSTI). Companion to Wave C/Addendum. | see table below |

### WAVE_C_DESIGNS_2026-07-10.md — the 15 items (doc says "11 turnkey")

1. **C-SEC-1** (HIGH, live) — `denied` permission grants never subtracted from allowed capabilities.
2. **C-SEC-1b** (MED, latent) — STS re-delegation TTL ceiling not clamped on stored spec.
3. **C-RELOAD** (HIGH/MED) — symlink/`..` bypass in `is_protected_path` + non-blocking reload mutex/debounce.
4. **C-SELFIMP** (HIGH) — code-tier self-improve apply has zero `approval_id` gate.
5. **C-GATEWAY** (MED×2) — no provider timeout on gateway ctor sites + SSRF alias leak in exception text.
6. **C-INTEGRITY** (MED) — non-canonical `"|".join` HMAC payload → path-boundary forgery.
7. **C-ENGINE** (HIGH verify + MED×3) — dead sync `execute()`, blocking `_run_tests` racing deferred commit, undrained `_background_tasks`.
8. **C-GITAUTO** (MED/LOW + CONFIRMED correction) — raw `subprocess.run` bypassing repo lock/timeout; tag dash-injection; worktree lock buys **zero** protection (gitlink `.git` is a file, `isdir` check silently fails).
9. **C-TOOLLOOP** (MED×2/LOW) — no per-response tool-call cap, no arg-schema pre-validation, `"last"` name collides with a dispatch sentinel key.
10. **C-SPD1** (MED) — `SpendRepository.add()` has zero callers; spend state lost across daemon restart.
11. **C-BUDGET** (MED×3/LOW) — reservation TOCTOU, daily rollover doesn't clear reservations, `projected_cost` defaults to 0 at 3 sites, restore has no dedup.
12. **C-EVENTLOOP** (MED×2, 1 CLOSED) — ~20 unbounded `to_thread` sites + unbounded dispatch gather; tick-pin item **VERIFIED CLOSED**.
13. **C-TODOMODEL+C-FILESTORE** (D-07-b) — 13 unbounded Text columns need caps + migration 027; filestore decompression-bomb + non-atomic write.
14. **C-CONNECTORS+C-WORKERAUTH** (LOW, 1 CLOSED) — NaN/Inf timestamp poisons window-anchor; PSK auth fail-closed **VERIFIED CLOSED**.
15. **C-LANGGRAPH** (MED dormant/LOW) — LangGraph path bypasses budget billing entirely (flag off); `allow_auto_promote` dead knob, recommend delete.

### WAVE_C_ADDENDUM_2026-07-10.md

**Already-fixed (verified, do not rework):** integrity store signing/anti-rollback; per-project secret isolation (the "unscoped" premise was stale); daemon bind/`allowed_cidr`.

**Still-open (6 new):** C12 EventBus no-locking/list-mutation/unbounded webhooks (MED); admin API rate-limit + body-cap gap (MED, = H-RATELIMIT below); rg_search unconfined root + unbounded output (MED, = H-RGSEARCH below); runtime bundle signing self-referential/no external sig (HIGH); ansible `process_isolation` missing-binary crashes daemon via uncaught `SystemExit` (HIGH); `TaskReturnRepository.get_by_id` unscoped + `TodoRepository` status mass-assign (MED/LOW).

### HARDENING_BACKLOG_2026-07-10.md — all 33 items

| ID | Round | Severity | Description | Status |
|---|---|---|---|---|
| C22-SSTI | R1 (ungrouped) | CRITICAL | bare Jinja2 `Environment` → project-shadowed template RCE | **FIXED** 2026-07-10 (commit `51f51f06`) |
| H-RGSEARCH | R1 | MED | `rg_search` unconfined root + unbounded output | LATENT (zero live callers) |
| H-RATELIMIT | R1 | MED | admin API has no body-size cap or rate limiter | OPEN |
| H-DAST | R1 | n/a | `host_is_blocked` wrong gate for DAST; needs allowlist+hard-deny | OPEN/unwired |
| H-WORKERBCAST | R1 | M2 | `worker_broadcast` dict-mutation-during-iteration, no lock | LATENT (no callers) |
| H-SELFUPD-TOCTOU | R1 | F2+F3 | self_update apply-path symlink/cwd-anchor TOCTOU bugs | CONFIRMED |
| H-ACCOUNTING | R1 | C18 | blocking git subprocess on loop + non-finite ledger values | CONFIRMED |
| H-PERM-NET | R1 | (unstated) | `_intersect_constraints` silently widens net constraints | CONFIRMED |
| H-CONNECTOR | R1 | C23/HIGH×2 | DNS-rebind TOCTOU + missing response caps, 47 fetch sites | CONFIRMED |
| H-FETCH-CAPS | R2 | MED | unbounded network fetches outside hardened set | CONFIRMED |
| H-FILE-CAPS | R2 | MED | unbounded whole-file reads of target-repo/attacker paths | CONFIRMED |
| H-TAR-BOMB | R2 | LOW | bootstrap tar extraction, no decompressed-size/member cap | CONFIRMED |
| H-WEBFETCH-REDIRECT | R2 | MED | SSRF guard bypassable via redirect | **FIXED** (commit `2f623702`) |
| H-SELFMOD-DENYLIST | R3 | MED-HIGH | AGENTS.md/CLAUDE.md/TASKS.md missing from self-mod deny-lists | CONFIRMED |
| H-ENGINE-WRITE-GUARD | R3 | MED | engine write path has no deny-list backstop | CONFIRMED |
| H-RELOAD-BATCH-F1 | R3 | LATENT | hot-reload batch path bypasses single-module guards | CONFIRMED |
| H-WEBRETRIEVE-DOS | R3 | MED-HIGH | sync fetch + unclamped timeout stalls event loop | CONFIRMED |
| H-WEBRETRIEVE-REBIND | R3 | MED | `fetch_web_page` vulnerable to DNS rebind | CONFIRMED |
| H-MCP-ARGCAP | R3 | MED | tool-call args decoded with no size cap | CONFIRMED |
| H-OTEL-TLS | R3 | MED | OTLP exporter hardcodes `insecure=True` | CONFIRMED |
| H-DB-TENANT-SCOPING | R3 | MED-HIGH (broad) | ~15 repository methods unscoped by `project_id` | CONFIRMED |
| H-STREAM-TRAVERSAL | R4 | HIGH | `/admin/stream/dispatch` RoleCloner path-traversal | CONFIRMED |
| H-MERGE-WRITER-SYMLINK | R4 | HIGH | merge-writer no containment check on write target | PLAUSIBLE, def-in-depth |
| H-RELOAD-BATCH-TRAVERSAL | R4 | HIGH | batch reload path builds path w/ no containment check | PLAUSIBLE, low exploitability |
| H-DISPATCH-SEMAPHORE | R4 | MED | dispatch fan-out gather has no semaphore bound | CONFIRMED |
| H-ANSIBLE-NOTIMEOUT | R4 | MED | main-path ansible subprocess has no wait_for timeout | CONFIRMED |
| H-TEARDOWN | R4 | MED | daemon teardown gaps beyond ExecutionEngine drain (3 sub-issues) | CONFIRMED |
| H-WRITER-DRAIN-UNWIRED | R5 | HIGH | subprocess-writer queued DB writes never drained/applied | LATENT (unreachable in prod) |
| H-ADMIN-PSK-ONLY | R5 | HIGH | entire `/admin/*` surface gated only by shared PSK, no RBAC | OPEN |
| H-BINARY-BUNDLING-GAPS | R5 | MED | rg/tofu/osquery bundled-binary machinery not actually used | OPEN (see BINARY_BUNDLING.md §1) |
| H-GIT-PUSH-REMOTE | R6 | MEDIUM | push helpers vet remote for shell-injection only, not SSRF/URL | CONFIRMED |
| H-MIGRATIONS-DECORATIVE | R6 | HIGH | production alembic migrations never run `upgrade()` body | CONFIRMED |
| H-OBS-TENANT-LEAK | R7 | HIGH | observability/reporting endpoints leak cross-tenant data (PSK has no tenant identity) | CONFIRMED |

**Total: 33 items** (32 H-prefixed + C22-SSTI). Note: two items above (C22-SSTI, H-WEBFETCH-REDIRECT) are marked FIXED here based on commits landed *after* the backlog doc was authored — cross-check against §5 before assuming the doc's own in-text status is current.

---

## 3. Severity-ranked action list (all docs)

### CRITICAL

| Item | Source | Status |
|---|---|---|
| **CRIT-1** — `.gludd/collections/` project-tier shadows bundled `general_ludd.*` roles by FQCN → in-process RCE | ANSIBLE_COLLECTION_TRUST.md | **OPEN** — land as one batch |
| **C22-SSTI** — bare Jinja2 Environment, project-shadowed template RCE | HARDENING_BACKLOG (R1) | **FIXED** 2026-07-10, commit `51f51f06` |
| **C-SEC-1** — `denied` permission grants never subtracted (prod-signing-key carve-outs inert) | WAVE_C_DESIGNS (doc rates this HIGH, "live today"; grouped here per its blast radius across permissions/sts/secrets) | **OPEN** |

### HIGH

| Item | Source | Status |
|---|---|---|
| H-ADMIN-PSK-ONLY — entire `/admin/*` gated by shared PSK only, no RBAC | HARDENING_BACKLOG R5 | OPEN |
| *"HumanTodo-ownership"* | referenced in task brief; **not located** in the surveyed docs/design corpus or in a targeted `HumanTodo` code grep (only normal usage found) — flag for the brief's author to confirm doc location, not fabricated here | UNCONFIRMED |
| H-STREAM-TRAVERSAL — RoleCloner path-traversal at `/admin/stream/dispatch` | HARDENING_BACKLOG R4 | OPEN (CONFIRMED) |
| H-MIGRATIONS-DECORATIVE — production alembic migrations never run `upgrade()` body | HARDENING_BACKLOG R6 | OPEN (CONFIRMED) |
| H-WRITER-DRAIN-UNWIRED — subprocess-writer queued DB writes never drained | HARDENING_BACKLOG R5 | OPEN (LATENT — unreachable in prod today) |
| H-DB-TENANT-SCOPING — ~15 repository methods unscoped by project_id | HARDENING_BACKLOG R3 | OPEN (CONFIRMED) |
| H-OBS-TENANT-LEAK — observability endpoints leak cross-tenant data | HARDENING_BACKLOG R7 | OPEN (CONFIRMED) |
| C-SELFIMP — self-improve code-tier apply has zero approval_id gate | WAVE_C_DESIGNS | OPEN |
| C-ENGINE — dead sync execute(), blocking `_run_tests`, undrained background tasks | WAVE_C_DESIGNS | OPEN |
| C-RELOAD — symlink/`..` bypass + reload-mutex race | WAVE_C_DESIGNS | OPEN |
| H-MERGE-WRITER-SYMLINK / H-RELOAD-BATCH-TRAVERSAL | HARDENING_BACKLOG R4 | OPEN (PLAUSIBLE, defense-in-depth priority) |
| Runtime bundle signing self-referential (no external sig) | WAVE_C_ADDENDUM | OPEN |
| Ansible `process_isolation` missing-binary crashes daemon (uncaught `SystemExit`) | WAVE_C_ADDENDUM | OPEN |
| Issue-source SSRF: 5 adapters (linear/trello/azure_boards/asana/servicenow) let cloud-metadata hosts through | ISSUE_SOURCES_SSRF_AND_BOUNDED_HTTP.md Part A | OPEN |

### MEDIUM

C-SEC-1b, C-GATEWAY (timeout + alias leak), C-INTEGRITY (HMAC canonicalization), C-GITAUTO (lock/timeout gap + worktree-lock-is-inert), C-TOOLLOOP, C-SPD1, C-BUDGET, C-EVENTLOOP (unbounded to_thread — tick-pin item already closed), C-TODOMODEL+C-FILESTORE, C-LANGGRAPH (dormant), C12 EventBus locking (addendum), H-RATELIMIT, H-RGSEARCH, H-DAST, H-WORKERBCAST, H-FETCH-CAPS, H-FILE-CAPS, H-SELFMOD-DENYLIST, H-ENGINE-WRITE-GUARD, H-RELOAD-BATCH-F1, H-WEBRETRIEVE-DOS, H-MCP-ARGCAP, H-OTEL-TLS, H-DISPATCH-SEMAPHORE, H-ANSIBLE-NOTIMEOUT, H-TEARDOWN, H-BINARY-BUNDLING-GAPS, H-GIT-PUSH-REMOTE, `TaskReturnRepository.get_by_id` unscoped (addendum).

### LOW

H-TAR-BOMB, GA-tag-dash-injection, C-LANGGRAPH's `allow_auto_promote` dead-knob deletion, `TodoRepository` status mass-assign (addendum).

### ALREADY FIXED (do not rework — verify via commit/measurement before touching)

| Finding | Evidence |
|---|---|
| SSTI (C22-SSTI) | commit `51f51f06` |
| web_retrieve SSRF + redirect-bypass (H-WEBFETCH-REDIRECT and related) | commit `2f623702` |
| vSphere TLS verify-by-default | commit `02db33f5` |
| unit-2 CI race #1 (env-redirect/tool-counts hook paths) | commit `6f5b9222` |
| unit-2 CI race #2 (gate-cleanup watchdog tmp isolation) | commit `20cbc0e4` |
| caplog fileConfig xdist pollution | prior session (per memory `gludd-ci-green-infra-fixes`), not a commit from this session's log window — re-verify if citing |
| Integrity store signing/anti-rollback | WAVE_C_ADDENDUM verified-already-fixed |
| Per-project secret isolation ("unscoped" premise was stale) | WAVE_C_ADDENDUM verified-already-fixed |
| Daemon bind/`allowed_cidr` | WAVE_C_ADDENDUM verified-already-fixed |
| C-EVENTLOOP tick-pin sub-item | WAVE_C_DESIGNS — VERIFIED CLOSED |
| C-CONNECTORS/C-WORKERAUTH: PSK auth fail-closed | WAVE_C_DESIGNS — VERIFIED CLOSED |

---

## 4. Landing order / parallelism

Full detail: [PARALLEL_IMPLEMENTATION_AND_PARITY.md](PARALLEL_IMPLEMENTATION_AND_PARITY.md). Its corrected matrix (supersedes an earlier draft table in the same doc) found **7 hot-collision files/packages**, not the 5 originally assumed:

| File/package | Why hot | Designs touching it |
|---|---|---|
| `security/permissions.py` + `sts.py` + `secrets/manager.py` | deny-enforcement fix chain | C-SEC-1 → C-SEC-1b |
| `models/gateway.py` | **2** designs (not 3 — D-DEPLOY does NOT touch it, per corrected re-check) | C-GATEWAY → C-BUDGET |
| `event_loop/loop.py` | **4** pending (SSTI already landed, dropped from live set) | C-SPD1 (first, PHASE_ORDER 17→18) then C-EVENTLOOP + C-LANGGRAPH in parallel |
| `execution/engine.py` | C-ENGINE + P-1 toolchain wiring must sequence after it | C-ENGINE, P-1 |
| `execution/tool_loop.py` | tool-loop + variable_store | C-TOOLLOOP |
| `daemon.py` | **newly added** to the matrix — 3-4 designs | C-ENGINE shutdown wiring, C26, H-RATELIMIT, C-BUDGET reinforcement |
| `self_update/*` | **newly added** — 4 designs | C9 deny-list, C-RELOAD, C-SELFIMP Hole1, H-SELFUPD-TOCTOU |

**Wave structure** (3 waves, not ~20 flat batches — Wave 1 itself is the ~9-batch parallel width):

- **Wave 1** (~9 fully parallel, file-disjoint batches): C-SEC-1, C-SELFIMP, C-RELOAD, C-TOOLLOOP, C-GITAUTO, C-INTEGRITY, C-CONNECTORS, C-TODOMODEL+migration 027, C-FILESTORE.
- **Wave 2** (parallel, disjoint from Wave 1): C-GATEWAY, C-SPD1, C-ENGINE.
- **Wave 3** (dependent tail, re-derive line numbers post-Wave-2): C-BUDGET (after C-GATEWAY), C-EVENTLOOP + C-LANGGRAPH (after C-SPD1, parallel to each other), `allow_auto_promote` delete (after C-SELFIMP).
- Gate rule: `make gate-async` → `make gate-status` green required between every wave; never start wave N+1 red.
- Security work (Wave 1) runs **concurrently with**, not strictly before, feature work — P-1 (generic toolchain runner, KEYSTONE) is explicitly scheduled in parallel with Wave C.
- P-1 must land before/coordinate with C-ENGINE (both touch `execution/engine.py`); P-2/P-3/P-4 depend on P-1; P-9 (worktree parallelism) is reported **already live** end-to-end via Ansible — correct any "dead" claim if citing P-9 status.

---

## 5. Already-shipped this session

| Commit | What |
|---|---|
| `89196585` | docs: browser-automation spec + round-3 hardening landing-order and addendum corrections |
| `20cbc0e4` | fix: isolate gate-cleanup watchdog tests to tmp_path — kills unit-2 xdist shared-file race |
| `7624be03` | style: ruff E501/F401/RUF012 + EOF fixes in web-retrieval and SSTI test files |
| `af1ee08f` | docs: hardening backlog, DAST, logging, deploy-saturate designs, Wave C corrections, mobile deck overhaul |
| `02db33f5` | fix: vSphere terraform TLS verification secure-by-default (`vsphere_verify_ssl`) |
| `51f51f06` | fix: sandbox jinja2 template rendering — closes project-shadowed template SSTI RCE |
| `2f623702` | fix: guard web_retrieve against SSRF + redirect-bypass; sandbox-error guard on langchain prompt adapter |
| `6f5b9222` | fix: env-redirect block-counter reason + tool-counts hook paths — kills unit-2 xdist shared-tmp race |
| `e43bf999` | docs: C-ENGINE async-execute migration corrections to round-2 addendum |
| `84b7d987` | style: wrap two hook-counter test lines under 120 cols |

(`master` is clean at survey time; the 4 untracked docs — ANSIBLE_COLLECTION_TRUST.md, BINARY_BUNDLING.md, PIPELINE_INTERACTION_ROLES.md, TIME_TIMERS_SCOPED_NOTIFICATIONS.md — plus modifications to CI_PIPELINE_MEDIC_ROLE.md, HARDENING_BACKLOG_2026-07-10.md, and WAVE_C_ADDENDUM_2026-07-10.md are still uncommitted as of this index.)

---

## Cross-spec implementation sequencing

Verified: **no blocking contradictions** among the 6 feature specs (LOGGING_CONFIGURATION,
CI_PIPELINE_MEDIC_ROLE, BINARY_BUNDLING, BROWSER_AUTOMATION, TIME_TIMERS_SCOPED_NOTIFICATIONS,
PIPELINE_INTERACTION_ROLES) — friction is file-collision/sequencing, not architecture.

**Rollout DAG (recommended order):**

1. LOGGING_CONFIGURATION — independent, 0 new deps, land first (aids everything's rollout observability)
2. CI_PIPELINE_MEDIC — independent, parallel-safe anytime
3. BINARY_BUNDLING-core (opentofu/osquery/ripgrep/node resolver bundle-first fix) — independent
4. BROWSER_AUTOMATION — adds `playwright` dep
5. BINARY_BUNDLING-playwright-section — **HARD dep on #4** (chromium bundling needs BROWSER's playwright choice)
6. TIME_TIMERS (internal order: T-1 resolver → T-2 Timer/scheduler → T-3 ScopedNotifier → T-4/T-5; **fold T-6's `recurring_rrule` column into T-2's migration**, not a 2nd migration)
7. PIPELINE_INTERACTION-core — parallel-safe with #6
8. PIPELINE `approve_gate` ScopedNotifier upgrade — **SOFT dep on T-3**; land now against flat HumanGate + follow-up patch otherwise

**Shared-file serialization points** (single-writer, sequential merge diffs even if dev happens in parallel):

| File | Order | Why |
|---|---|---|
| `config/user_config.py` | any order (additive) | 4 specs add sibling nested fields (logging/browser/pipeline_drive/timekit+timers) — land as sequential rebased diffs |
| `daemon.py` | LOGGING → BINARY_BUNDLING → BROWSER → PIPELINE | correlation bind/log-level, then boot hook, then `/api/browser` router, then `/api/pipeline` router + `wire_pipeline_drive` |
| `event_loop/loop.py` | TIME_TIMERS first, LOGGING second | TIME_TIMERS inserts a "fire_timers" phase (bumps PHASE_ORDER, touches 4 phase-count test files) — LOGGING's run_context wrap is non-structural, avoid re-touching those 4 test files twice |
| `pyproject.toml` | BROWSER before BINARY_BUNDLING-playwright; TIME_TIMERS in one pass | TIME_TIMERS adds 7 core deps + optional astronomy extra + 6 mypy-override lines together; `make relock` after each landing |

**Aggregate new deps:** `playwright>=1.44` (heavy, ~150MB chromium, gated `GLUDD_ENABLE_BROWSER`); TIME core: `dateparser`, `tzdata`, `holidays`, `workalendar`, `astral`, `python-dateutil`, `humanize`; TIME optional astronomy extra: `skyfield`, `convertdate` (pulls numpy). LOGGING/BINARY_BUNDLING/PIPELINE_INTERACTION/CI_PIPELINE_MEDIC add **zero** Python deps. `playwright` and `skyfield` are the two heavy items — both correctly opt-in-gated.

**Two decisions flagged (not bugs):**
(a) two secrets conventions — BROWSER uses `SecretsManager.resolve(alias)`, PIPELINE uses token_env-name-in-config (each matches its own domain precedent); decide whether to unify.
(b) two SSRF gating shapes — BROWSER is allowlist-primary + default-deny (per-call agent URL), PIPELINE is `allow_private` opt-in + pre-registered allowlist (no per-call URL); justified by differing threat models — don't force one onto the other.

---

## 6. Pre-existing docs/design/ files (out of scope for this pass)

Listed for completeness only — not summarized here; see [`docs/design/index.md`](index.md) for the older curated index:

AGENT_HIBERNATION.md, BATCH5_ROADMAP.md, COLLECTION_STRUCTURE.md,
CONTEXTUAL_LOGGING_AND_ERROR_REPRO.md, HUMAN_TODOS.md, MCP_CATALOG_OOM_FIX_SPEC.md,
MODEL_SERVING_DEPLOYMENT.md, OPENBAO_BREAK_GLASS_BACKUP.md, PERMISSION_SYSTEM.md,
PLAYBOOK_WEB_RENDERER.md, PROJECT_COLLECTIONS.md, PROJECT_LOCAL_GLUDD_DIR.md,
PROJECT_RUNNER.md, REMEDIATION_SYSTEM.md, SANDBOX_BACKENDS.md, SLM_COMPACTION.md,
SYMBIOTIC_AGENT_INTEGRATION.md, TERRAFORM_INFRA_STRUCTURE.md, backlog_audit_system.md,
collection-structure.md, connector_join_key_normalization.md, connector_wiring_plan.md,
daemon_integration_plan.md, feature_gap_backlog.md, feature_package_wiring.md,
git_execution_architecture.md, index.md, issue_sources.md, observability_receiver.md,
observe_debugging_roles.md, per_model_prompt_adapter.md, permission-system.md,
pipeline_controller.md, queue-lease-audit.md, self_update_router.md,
symbiotic-agent.md, tool_calls_via_ansible.md, unified_data_source_relevance.md.
