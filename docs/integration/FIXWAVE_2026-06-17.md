# Fix wave 2026-06-17 — branch ledger + consolidated merge plan

All branches below are cut from master `3223c67`, committed via `make commit-bootstrap`
(no-gate; fast pre-commit hooks only). They have DISJOINT file sets by design, so the
consolidated merge should be additive/clean. Plan: merge ALL onto one integration branch
off master, run ONE `make gate` (~29 min), then ff master. Do NOT gate each branch
separately (basetemp stampede + wasted time).

## Landed (commit + branch + scope)
- [x] mainthread-budget hook — `86d037a` `feature/mainthread-budget-hook` — new `.claude/hooks/mainthread_budget.sh` + `make seed-mainthread-hook` + 18 tests. (The hook + settings.json edits also exist in the working tree.)
- [x] quality dedup — `dc8e58d` `worktree-agent-ab005c42cf52d51ef` — quality/tools.py shim delegates to QualityGateChecker (molecule left independent, correctly).
- [x] db/repository dedup — `076e412` `worktree-agent-a3aacc91759dc1d90` — `_scope` (13 guards) + `_count_by_fields`; `_guarded_update` intentionally NOT extracted (divergent semantics).
- [x] secrets dedup — `7ba5cf6` `worktree-agent-ae3657abbc7eff90f` — `SecretsManagerProtocol` + `_read_kv_secret` + `_strip_and_require`; 58 tests.

## In flight (await commit hash)
- [ ] budget gating B1/B3/B4/B5 — task add0fd039 — daemon.py, worker/app.py, routers/models.py, execution/engine.py, daemon_wiring.py, models/gateway.py. SECURITY/COST critical.
- [ ] SSRF host-deny dedup — task a96aded5 — new security/ssrf.py + auth.py + sanitize.py + connectors/base.py. SECURITY (closes blocklist drift: instance-data, 100.100.100.200).
- [ ] local-inference/slurm command dedup — task a372a6a3 — infra/local_inference.py, infra/slurm.py.
- [ ] mcp validator-factory dedup — task a5ab8ee9 — mcp/config.py, registry.py, catalog.py, new mcp/_validators.py.
- [ ] /api/status host-path removal (P1 security) — task a15126f6 — routers/todos.py + new tests/unit/test_todos_status_redaction.py.
- [ ] models routing/helpers dedup — task a7feeaae — models/scoring/router.py, langgraph_gateway.py, auto_configurator.py.

## Deferred to wave 2 (overlap in-flight files — dispatch AFTER budget/ssrf/host-path land)
- gateway.py non-retryable-set dedup (#2) — overlaps budget writer's gateway.py.
- routers/models.py gateway-construction/serialization/json-guard dedup (#3/#5/#6) — overlaps budget writer.
- execution/engine.py diff-parse + git-subprocess boilerplate dedup (#6/#7) — overlaps budget writer.
- event_loop/loop.py budget-block + prompt-registry + pid dedup (#1/#8/#10) — touch budget-adjacent code.
- daemon.py _lifespan budget-config triple-read dedup (#3/#5) — overlaps budget writer.
- filestore bootstrap.py list_binaries alias — callers include routers/todos.py (overlaps host-path writer).

## Separate effort (feature worktree branches, not on master yet)
- WEB toolkit (worktree wf_fbc35860-2a9-7, 46de4c8): W1 Playwright DNS-rebinding (HIGH), W2 overall_deadline not enforced (HIGH), W3 render_js skips detect_block (MED), W4 _verify_peer silent pass (LOW).
- COMPUTE discovery (worktree wf_23ba9683-564-5, c886e99): C1 AWS auth masked as PARTIAL (HIGH), C2 no per-source enable/disable (HIGH), C3 gather(return_exceptions=False) (MED).

## Adversarial verifier findings → WAVE 3 (post-merge follow-ups, NOT merge blockers)
Budget branch (ad8043d) — verified net-improvement, safe to merge; harden after merge (touches engine.py owned by this branch + new reviewer.py):
- B4 fail-OPEN default: execution/engine.py `_budget_pre_check` uses `verdict.get("allowed", True)` and returns None(allow) on unknown guard shape → change to default-DENY (allowed=False on missing key; deny on unknown shape when a guard IS injected; keep guard-is-None as intentional no-op).
- ReturnReviewer ungated: review/reviewer.py:139 `_call_model` calls gateway.call_model with no check_all_limits pre-check (EventLoop gates review-job dispatch upstream + gateway records spend post-hoc, so partially defended). Add a pre-check.
- event_loop generation path: job_invocation.py:68 `invoke_model_for_generation` has no pre-check; confirm the loop.py caller gates it (worker path IS gated via worker/app.py:241).
- B5 degraded-startup edge: routers/models.py B5 fail-opens if lifespan partially fails and `_budget_guard` never set. Consider fail-closed in degraded mode.
- B1 tests assert storage/wiring only — add a test that record_spend actually fires on a billed call.

SSRF branch (7cb2c82) — verified safe to merge (imports clean, arg-order correct, scheme policy intact, base.py is strict superset). Follow-up:
- ~20+ per-connector files (jenkins.py, k8s_events.py, newrelic.py, elasticsearch.py, victoriametrics.py, etc.) still carry their OWN local _BLOCKED_HOSTNAMES guards that bypass connectors/base.py.is_safe_endpoint; some are incomplete (jenkins.py missing metadata names). Re-point them all to canonical security.ssrf.host_is_blocked (needs SSRF branch merged first so the module exists on master). Also prom_scrape.py blocks fd00:ec2::254 (AWS IPv6 metadata) — fold into canonical BLOCKED_METADATA_IPS.

Other wave-3 (from deeper DRY sweep, deferred — overlap/ordering):
- cli.py HTTP-dispatch boilerplate (~50 _cmd_* handlers) → `_http_call` helper (after cli-tables branch merges; same file).
- git_automation/repo.py #3: _validate_daemon_host (cli.py) + repo.py _host_is_blocked duplicate SSRF logic → use canonical security.ssrf (after SSRF merges).
- filestore/bootstrap.py: remove list_binaries_with_versions alias + update cli.py:843 caller (after cli + host-path merged).

## Subagent-architecture wave (multitasking infra, made first-class tracked code)
- mt-1 mainthread_budget hook — 86d037a feature/mainthread-budget-hook (18 tests)
- mt-3 gate concurrency lock — fb08eb9 worktree-agent-a949c699c2a63c2c5 (scripts/run_gate.sh flock + per-run mktemp basetemp + cleanup trap; Makefile gate rewired; 12 tests)
- mt-4 tracked agent_liveness + floor_planner — 55000e8 worktree-agent-a4a31a3f2578c3011 (43 tests)
- mt-6 agent_watchdog (mid-task-rest detector) — 5d11bb2 worktree-agent-ac32b608bad61e477 (28 tests)
- mt-8 enforced multitasking backlog — 5e4d05f worktree-agent-a22cc1847462f8141 (scripts/multitasking_backlog.json + _check.py --assert-done, anti-rubber-stamp; 33 tests) + .claude/hooks/multitasking_backlog_stop.sh registered (fail-open until script on master)
- mt-5 (gate-authorization guard: block subagents running unauthorized full gates) — OPEN, blocked on fb08eb9 merge (builds on run_gate.sh).
- mt-7 (predictive floor) — covered by floor_planner.

### MAKEFILE RECONCILIATION (at integration time)
Three Makefile touchers: (a) my uncommitted main-tree edits (the pre-existing +43 lines incl. ps-pytest/kill-stale, PLUS an incident-hotfix kill-gate-force WITHOUT a .PHONY entry); (b) gate-lock branch fb08eb9 (canonical kill-gate-force WITH .PHONY + gate->run_gate.sh rewiring); (c) hook branch 86d037a (seed-mainthread-hook target). RESOLUTION: take the gate-lock branch's kill-gate-force (canonical) and DISCARD my main-tree hotfix version; keep ps-pytest/kill-stale; keep the hook seed target. All additive otherwise.

## Adversarial-verify findings → follow-up hardenings (branches are net-positive + safe to merge; fix AFTER ff)
All 15 branches adversarially verified pre-merge. Confirmed-solid: budget(net-improvement), SSRF, db/repository (no wrong model class; OR-guards correctly preserved), models-routing (selection equivalent), mcp/quality/schemas/git_automation/local_inference/cli-tables (in flight, expected clean). Follow-ups found:
- HOST-PATH (15263d2): (a) cli.py:919-922 `_cmd_status` still reads removed keys config_dir/config_files/filestore_root from the live API -> prints "not set"/blank (introduced CLI regression — fix after host-path+cli-tables merge, touches both files); (b) todos.py:216 `db_engine` emits str(engine) which can carry the password — PRE-EXISTING on master, redaction incomplete, sanitize it; (c) redaction test OR-condition too permissive (tighten to exact masked output + assert db_engine clean); (d) filestore_available=bool(root_path) truthy for nonexistent path.
- MODELS-ROUTING (2f03c7f): `_coerce_sample_count` returns 0 for non-numeric sample_count (e.g. "N/A") -> skips the row, whereas master raised and aborted routing. DELIBERATE hardening (more robust) — ACCEPT, but document; if exact-equivalence required, revert to bare int().
- SECRETS (7ba5cf6): (a) scan_for_image_updates lost its outer try/except -> a "Not connected" RuntimeError now escapes un-wrapped instead of SecretsUnavailableError (restore the wrapper); (b) exception message text changed ("unavailable resolving alias"->"unavailable for alias", "unavailable reading"->"unavailable for path") — restore original format (gate will flag if a test asserts it). NO secret leak (verified).

## WAVE 3 — connector SSRF consolidation (CORRECTED full scan of master connectors/, 18 source files)
Every one of the 18 connector files carries its OWN local host/IP SSRF guard (not delegating to base.is_safe_endpoint); ALL 18 are missing `instance-data` and `100.100.100.200`. TWO LIVE SECURITY BUGS on master (loopback bypass), fix with priority:
- dynatrace.py + influxdb.py: use `host.endswith(".localhost")` which does NOT catch `localhost.localdomain`.
- sentry.py + elasticsearch.py: omit `localhost.localdomain` from the blocklist entirely.
Plan: ONE branch (after SSRF 7cb2c82 merges so security.ssrf exists) re-pointing all 18 to canonical `security.ssrf.host_is_blocked`/`is_url_blocked`, preserving each connector's http+https scheme policy and the allow_private opt-in (tempo/zipkin/okta). Full per-file edit recipe is in the 2026-06-17 transcript (connector re-scan ada439f6). Files ranked most-incomplete-first: sentry, jaeger, tempo, zipkin, okta, influxdb, dynatrace, jenkins, newrelic, honeycomb, pagerduty, opsgenie, circleci, github_actions, gitlab_ci, azure_devops, buildkite, victoriametrics, elasticsearch, datadog, prometheus, splunk.

## Context
- The old `integration/batch45-testfix` line (base 995c194) is STALE/superseded — do NOT merge it. The 7 "gate failures" on it were stale-branch artifacts (tests 1-6 pass on master; test 7's file is absent — host-path fix above ports the real gap). See REBASELINE_2026-06-17.md.
- Budget verify: B2 already fixed on master (e982a81); B1/B3/B4/B5 still open → the budget branch above.
