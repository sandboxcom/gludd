# Integration Wave Follow-Up Backlog — 2026-06-17

Deferred items captured so they are not lost. Ordered by priority: ship blockers first,
then correctness/wiring, then cleanup.

## P0 — Ship blockers

### 1. Relocate + rotate `sandboxcom_github_rsa`
A private SSH key is committed inside the repo tree. Move it out of version control and
rotate it before any release (per the SSH-key audit).
- Files: `sandboxcom_github_rsa` (repo tree), plus any consumer referencing its path.

### 2. Fill `RG_SHA256` placeholder in `bundle-ripgrep`
The Makefile `bundle-ripgrep` target ships a placeholder checksum, so integrity
verification is a no-op. Replace with the real ripgrep release SHA256.
- Files: `Makefile` (`bundle-ripgrep` target).

## P1 — Correctness / security wiring

### 3. A-4: force-close auth on `POST /api/dispatch`
Dispatch must require auth even when global auth is off. Implement the audit's
recommended scoped `GLUDD_ALLOW_UNAUTH_DISPATCH` escape hatch (default-deny) in the
daemon middleware.
- Files: `daemon.py` (auth middleware).

### 4. Wire `SaturationController` into the event loop
`controllers/saturation.py` is correct but unwired — the claim/dispatch path never
consults it. Wire it into the loop and export it from the controllers package.
- Files: `controllers/saturation.py`, `controllers/__init__.py`, event-loop claim/dispatch path.

### 5. Consume per-queue PID buckets in dispatch
Dispatch currently honors only the global cap; it ignores
`desired_active_buckets_by_queue`. Apply the per-queue buckets so each queue is paced
independently.
- Files: dispatch path (saturation/loop wiring), `controllers/saturation.py`.

### 6. Verify `AutoBenchmarkRecorder` is wired to a real `BenchmarkRepository`
The `record_result` signature fix assumes a real `BenchmarkRepository` in production.
Confirm the production wiring actually injects one (not a stub/no-op).
- Files: `AutoBenchmarkRecorder` definition + its production wiring/factory, `BenchmarkRepository`.

## P2 — Dedup / cleanup

### 7. Consolidate duplicate `SelfImproveGate`
Two implementations exist; `loop.py` uses the new one. Pick a single source of truth and
delete the other.
- Files: `controllers/self_improve_gate.py`, `self_improve/gate.py` (`loop.py` consumes one).

### 8. Finish deferred batch-3 dedup work
The batch-3 dedup/coherence audit (`docs/audit/batch3_dedup_coherence.md`) found **8**
duplicate pairs. Most remain present in the tree. The full still-deferred set:

**Zero-risk deletions (dead code):**
- (a) Delete `issue_sources/markdown_source.py` (`MarkdownSource`). Zero references —
  no import, no test, no registry entry. Canonical is `issue_sources/markdown_todo.py`.

**Low-risk deletions (duplicate of a richer canonical; verify no registry ref first):**
- (b) Delete `connectors/windows_event.py` (`WindowsEventSource`) +
  `tests/unit/test_connector_windows_event.py`. Canonical is
  `connectors/windows_event_log.py` (`WindowsEventLogSource`) — richer runner tuple
  `(rc, stdout, stderr)`, more injection validation, 26 vs 12 tests.

**Deletions that require porting a security feature first (do NOT just delete — feature gap):**
- (c) Port `connectors/docker_api.py`'s `_confine_socket` guard into
  `connectors/docker_engine.py`, THEN delete `connectors/docker_api.py` +
  `tests/unit/test_connector_docker_api.py`. `docker_engine.py` is otherwise the richer
  canonical (Response dataclass, more tests, `Connector` alias), but the socket-confinement
  guard from `docker_api.py` is a real security feature that must be preserved.
- (e) Merge `issue_sources/excel_csv.py`'s `_confine(root, path)` realpath
  root-confinement guard into `issue_sources/csv_excel.py` (`CsvExcelSource`, the canonical
  IssueSource-family adapter), THEN delete `issue_sources/excel_csv.py` +
  `tests/unit/test_issue_source_excel_csv.py`. The confinement is absent from the canonical —
  a security gap, not pure duplication.

**Test re-point then delete:**
- (d) Re-point `tests/unit/test_pipeline_controller.py` to import from
  `pipeline.controller` / `pipeline.lanes` (the real async impl the daemon uses), THEN
  delete `orchestration/pipeline_controller.py` (synchronous design-time model, superseded).

**Backward-compat class removal:**
- (f) Remove the singular `GitHubIssueSource` "backward compat" class from
  `issue_sources/github_issues.py` and migrate/retire
  `tests/unit/test_issue_source_github.py`. No production caller; the canonical is the plural
  `GitHubIssuesSource` (IssueSource subclass).

**Design-decision pairs:**
- (g) `infra/misconfig_detector.py` vs `infra/model_deploy_check.py` — **decision made:**
  keep `model_deploy_check.py` (self-contained, more tests), port misconfig's rule (d) into
  it, then delete `misconfig_detector.py`. See
  `docs/audit/misconfig_detector_dedup_decision.md`.
- (h) `connectors/tempo.py` + `connectors/zipkin.py` vs `connectors/tempo_zipkin.py` —
  **decision pending.** Standalone pair has 63 tests vs 28 for the combined
  `TempoZipkinSource`; leaning toward keeping the standalones and deleting `tempo_zipkin.py`
  (or making it a thin adapter), but not yet decided.

**Already DONE (do not redo):**
- Missing `__init__.py` added for `src/general_ludd/pipeline/` and
  `src/general_ludd/issue_sources/`.

**Orphan-test re-points (4):** beyond (d) above, `test_issue_source_github.py`,
`test_connector_windows_event.py`, and `test_connector_docker_api.py` all import orphan/
duplicate modules and must be migrated or deleted alongside their target module (covered by
(b), (c), (f) respectively).

- Files: the modules listed per item above, their test files, and
  `docs/audit/batch3_dedup_coherence.md` / `docs/audit/misconfig_detector_dedup_decision.md`.
