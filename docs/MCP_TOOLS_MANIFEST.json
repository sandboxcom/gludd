[
  {
    "name": "general_ludd.agent.gludd_abtest",
    "description": "Crash-isolated A/B test of a candidate code variant in a fresh subprocess — Runs a baseline (A = current C(src)) and a candidate (B = candidate worktree) under the SAME workload, each in a FRESH interpreter child process via C(general_ludd.abtest.run_ab). The candidate is NEVER imported into this process, so a candidate built to crash the whole app (C(os._exit), segfault, infinite loop, OOM) CANNOT take down the Ansible controller or the daemon. Fail-closed: B is promoted ONLY if A passed and B ran ok, did not crash, did not time out, and stayed within a duration slack. Any crash/timeout yields C(promote=false). Runs in-process (same venv as C(general_ludd)); does NOT call the daemon.",
    "input_schema": {
      "type": "object",
      "properties": {
        "candidate_root": {
          "type": "string"
        },
        "baseline_root": {
          "type": "string"
        },
        "repo_root": {
          "type": "string",
          "default": "."
        },
        "module": {
          "type": "string"
        },
        "expect_attr": {
          "type": "string"
        },
        "timeout": {
          "type": "number",
          "default": 60.0
        },
        "mem_limit_mb": {
          "type": "integer",
          "default": 512
        },
        "verdict_path": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        }
      },
      "additionalProperties": false,
      "required": [
        "candidate_root",
        "module"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_accounting",
    "description": "Fetch per-project accounting snapshots via the daemon — C(state=all) calls C(GET /api/accounting) and returns accounting snapshots for ALL known projects as C(ansible_facts.gludd_accounting). C(state=project) calls C(GET /api/accounting/{project_id}) and returns the accounting snapshot for a single project. Both operations are read-only. PSK-authed; check-mode safe.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "all",
            "project"
          ],
          "default": "all"
        },
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_agent_run",
    "description": "Run the agent tool-call loop (prompt + tools → answer) — W6.8 decision: uses the existing C(ToolCallLoop) from C(execution.tool_loop) — langgraph/langchain are declared deps with zero production callers, so option (b) was chosen (keep ToolCallLoop; note that langgraph removal is deferred to W4.5 deps-audit). Accepts a prompt and optional tool list, iterates model/tool calls up to C(max_iterations) times, and returns the final answer and tool call history. Check mode skips the model call and returns a placeholder.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string"
        },
        "system_prompt": {
          "type": "string",
          "default": ""
        },
        "tools": {
          "type": "array",
          "default": []
        },
        "max_iterations": {
          "type": "integer",
          "default": 10
        },
        "model_profile": {
          "type": "string",
          "default": ""
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "prompt"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_break_glass",
    "description": "OpenBao raft snapshot and restore (break-glass backup) — Wraps the OpenBao HTTP API for the two break-glass endpoints C(/v1/sys/storage/raft/snapshot) (GET — snapshot) and C(/v1/sys/storage/raft/restore) (POST — restore). Mode C(snapshot) fetches the current raft snapshot bytes and writes them to C(output_path). Mode C(restore) POSTs the bytes from C(restore_source) back into a running OpenBao server. The C(token) argument is marked C(no_log=True); OpenBao tokens MUST NOT leak into Ansible task output. This module is safe to call from C(check_mode) for snapshot (it does nothing destructive); restore is refused in check_mode.",
    "input_schema": {
      "type": "object",
      "properties": {
        "openbao_addr": {
          "type": "string"
        },
        "token": {
          "type": "string",
          "x-no-log": true
        },
        "output_path": {
          "type": "string",
          "default": ""
        },
        "mode": {
          "type": "string",
          "enum": [
            "snapshot",
            "restore"
          ],
          "default": "snapshot"
        },
        "restore_source": {
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "required": [
        "openbao_addr",
        "token"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_db",
    "description": "Todo/resource CRUD via daemon HTTP API (never raw SQLite) — Performs todo and resource operations against the daemon's REST API. {'Supported ops': 'C(todo_get), C(todo_create), C(todo_update_status), C(resource_preference).'} C(todo_create) issues POST /api/todos on the daemon (never raw SQLite). NEVER opens the SQLite file directly (single-writer rule). Check mode skips write operations.",
    "input_schema": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "enum": [
            "todo_get",
            "todo_create",
            "todo_update_status",
            "resource_preference"
          ]
        },
        "todo_id": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "resource_type": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string",
          "default": ""
        },
        "work_type": {
          "type": "string",
          "default": "code"
        },
        "queue": {
          "type": "string",
          "default": "core"
        },
        "priority": {
          "type": "string",
          "default": "medium"
        },
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        },
        "role": {
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "required": [
        "op"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_dispatch",
    "description": "Interact with the daemon's dynamic-dispatch API — C(state=dispatch) POSTs a tool-call to C(POST /api/dispatch) with a C(kind)/C(name)/C(args) body and returns the dispatch result as C(ansible_facts.gludd_dispatch). C(state=available) calls C(GET /api/dispatch/available) and returns the list of dispatchable tool handlers. C(state=recent) calls C(GET /api/dispatch/recent) and returns the most recent dispatch records. PSK-authed; check-mode safe on C(state=available) and C(state=recent). C(state=dispatch) skips the API call in check mode and returns an empty result.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "dispatch",
            "available",
            "recent"
          ],
          "default": "available"
        },
        "kind": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "args": {
          "type": "object"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_embed",
    "description": "Embedding similarity over the daemon's bert surface — With C(op=similar) (the default) queries the daemon's read-only C(POST /api/embeddings/similar) endpoint and returns the ranked similar canonical task types under C(ansible_facts.gludd_embed) so a playbook (or the model running a job) can borrow a good model/prompt from a semantically-neighboring task type. With C(op=compare) queries C(POST /api/embeddings/compare) to measure the pairwise similarity of two strings (C(text_a)/C(text_b)) produced by separate bots/agents — so a role can decide how to proceed (near-duplicate strings -> merge/dedupe; divergent -> escalate). Supply C(texts) (2+) instead for the full pairwise similarity matrix. The snapshot is injected under C(ansible_facts.gludd_embed). With C(op=search) queries C(POST /api/embeddings/search) to take a string a bot produced (C(text)) and search a real corpus with it (RAG search), returning the C(top_k) most-similar items ranked by cosine similarity. C(corpus) selects the corpus — C(skills) (the live skill registry, descriptions matched on the fly), C(task_types) (the canonical task types), C(prompts) (the persisted prompt profiles), C(traces) (recent execution traces, work_type/phase/span descriptions matched on the fly), or C(events) (recent audit events, event_type/entity_type and a summary of the details JSON matched on the fly). The snapshot is injected under C(ansible_facts.gludd_embed). Read-only and check-mode safe — it performs no writes (C(changed=False)). Similarity is computed over the same embedding layer the adaptive router uses (HashEmbedder offline, OpenAIEmbedder when C(OPENAI_API_KEY) is set on the daemon).",
    "input_schema": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "enum": [
            "similar",
            "compare",
            "search"
          ],
          "default": "similar"
        },
        "text": {
          "type": "string"
        },
        "corpus": {
          "type": "string",
          "enum": [
            "skills",
            "task_types",
            "prompts",
            "traces",
            "events"
          ],
          "default": "skills"
        },
        "text_a": {
          "type": "string"
        },
        "text_b": {
          "type": "string"
        },
        "texts": {
          "type": "array"
        },
        "top_k": {
          "type": "integer",
          "default": 5
        },
        "work_type": {
          "type": "string"
        },
        "include_embedding": {
          "type": "boolean",
          "default": false
        },
        "include_embeddings": {
          "type": "boolean",
          "default": false
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_environment",
    "description": "Inject the consolidated environment + optimization brief as ansible_facts — Queries the daemon's read-only C(GET /api/environment) endpoint and returns the consolidated environment brief under C(ansible_facts.gludd_environment) so a playbook (or the model running a job) can see the environment it runs inside and how to optimize for the task. Read-only and check-mode safe — it performs no writes. Exposes C(models) (roster, NO secrets), C(routing), C(budget), C(compute), C(tools), C(skills), C(queues), C(system), and C(optimization) (advisor hints + per-work-type recommended profiles). The model roster NEVER contains api keys, tokens, or credential aliases.",
    "input_schema": {
      "type": "object",
      "properties": {
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        },
        "work_type": {
          "type": "string"
        },
        "prompt_tokens": {
          "type": "integer"
        },
        "priority": {
          "type": "string",
          "enum": [
            "cost",
            "quality",
            "latency"
          ],
          "default": "quality"
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_facts",
    "description": "Inject live daemon facts (work/todo/model/history/messages) as ansible_facts — Queries the daemon's read-only C(GET /api/facts) aggregation endpoint and returns the structured snapshot under C(ansible_facts.gludd) so a playbook can branch on live data in C(when:) / C(vars:). Read-only and check-mode safe — it performs no writes. Exposes C(gludd.work), C(gludd.todos), C(gludd.models), C(gludd.history), and C(gludd.messages).",
    "input_schema": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_features",
    "description": "Fetch and verify the feature database via the daemon — C(state=list) calls C(GET /api/features) and returns all features (optionally filtered by status/category) as C(ansible_facts.gludd_features). C(state=verify) calls C(POST /api/features/verify) to trigger the server-side FeatureVerifier pass and returns the verification summary + per-feature results. Both operations are read-only from the controller perspective; the daemon may persist verification results internally on a C(state=verify) call. PSK-authed; check-mode safe on C(state=list). C(state=verify) skips the API call in check mode and returns an empty summary.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "list",
            "verify"
          ],
          "default": "list"
        },
        "status": {
          "type": "string",
          "enum": [
            "requested",
            "implemented",
            "verified",
            "regressed"
          ]
        },
        "category": {
          "type": "string"
        },
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_gate_check",
    "description": "Check whether a .gate-status file is complete and passed — Reads the C(.gate-status) file written by C(make gate) and determines whether the gate run is complete (a terminal marker is present) and whether it passed. C(gate_complete) is C(true) when the file exists and contains either C(=== GATE: PASSED ===) or C(=== GATE: FAILED ===). C(gate_passed) is C(true) when the file exists, is complete, and contains C(=== GATE: PASSED ===). Check-mode safe — this module performs no writes. Wraps the same logic as C(scripts/gate_fresh_check.py).",
    "input_schema": {
      "type": "object",
      "properties": {
        "gate_path": {
          "type": "string",
          "default": ".gate-status"
        },
        "state": {
          "type": "string",
          "enum": [
            "check"
          ],
          "default": "check"
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_git",
    "description": "Run hardened git control-plane operations through Gludd — Sends a typed, authenticated request to the daemon-owned GitAutomation service. Collection Python never imports Gludd core or launches git itself. Mutations remain check-mode safe and preserve the historical result schema.",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "op": {
          "type": "string",
          "enum": [
            "init",
            "clone",
            "commit",
            "gated_commit",
            "current_branch",
            "branch",
            "branch_list",
            "branch_delete",
            "worktree_list",
            "worktree_create",
            "worktree_remove",
            "merge",
            "gated_merge",
            "push",
            "verify_remote",
            "tag_release",
            "tag_checkpoint",
            "release_tag",
            "checkpoint_tag",
            "state",
            "batch_push",
            "release_cut",
            "release_delete",
            "release_recut",
            "ci_verdict",
            "ci_cancel"
          ]
        },
        "clone_url": {
          "type": "string"
        },
        "target_dir": {
          "type": "string"
        },
        "git_clone_timeout": {
          "type": "integer",
          "default": 120
        },
        "clone_allow_local": {
          "type": "boolean",
          "default": true
        },
        "message": {
          "type": "string"
        },
        "files": {
          "type": "array",
          "default": []
        },
        "gate_cmd": {
          "type": "array",
          "default": []
        },
        "branch": {
          "type": "string"
        },
        "worktree_path": {
          "type": "string"
        },
        "source": {
          "type": "string"
        },
        "target": {
          "type": "string"
        },
        "strategy": {
          "type": "string",
          "enum": [
            "ff",
            "no-ff",
            "squash"
          ],
          "default": "ff"
        },
        "tag": {
          "type": "string"
        },
        "todo_id": {
          "type": "string"
        },
        "sha": {
          "type": "string"
        },
        "expected_sha": {
          "type": "string"
        },
        "ssh_key_path": {
          "type": "string"
        },
        "ref_type": {
          "type": "string",
          "enum": [
            "heads",
            "tags"
          ],
          "default": "heads"
        },
        "threshold": {
          "type": "integer",
          "default": 5
        },
        "force": {
          "type": "boolean",
          "default": false
        },
        "check_ci": {
          "type": "boolean",
          "default": true
        },
        "release_tag": {
          "type": "string"
        },
        "release_message": {
          "type": "string",
          "default": ""
        },
        "release_remote": {
          "type": "string",
          "default": "sandboxcom"
        },
        "release_repo": {
          "type": "string",
          "default": "sandboxcom/gludd"
        },
        "skip_readme_check": {
          "type": "boolean",
          "default": false
        },
        "skip_ci_check": {
          "type": "boolean",
          "default": false
        },
        "run_id": {
          "type": "string"
        },
        "remote": {
          "type": "string",
          "default": "origin"
        },
        "state_ref": {
          "type": "string",
          "default": ""
        },
        "state_gha_head_sha": {
          "type": "string",
          "default": ""
        },
        "state_worktree_target_ref": {
          "type": "string",
          "default": "HEAD"
        },
        "state_preserve_branch_patterns": {
          "type": "array",
          "default": []
        },
        "state_reconciled_preserve_heads": {
          "type": "array",
          "default": []
        },
        "state_reconciled_preserve_head_file": {
          "type": "string",
          "default": "config/reconciled_preserved_heads.txt"
        },
        "state_assert_clean": {
          "type": "boolean",
          "default": false
        },
        "state_assert_no_feature_on_master": {
          "type": "boolean",
          "default": false
        },
        "state_assert_merge_ready": {
          "type": "boolean",
          "default": false
        },
        "state_assert_remote_head": {
          "type": "boolean",
          "default": false
        },
        "state_assert_gha_matches_local": {
          "type": "boolean",
          "default": false
        },
        "state_assert_no_unintegrated_worktrees": {
          "type": "boolean",
          "default": false
        },
        "state_assert_no_unintegrated_branches": {
          "type": "boolean",
          "default": false
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 300
        },
        "idempotency_key": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "path",
        "op"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_human_todo",
    "description": "File or resolve a bot→human request (HumanTodo) via the daemon — Agents use this module to ask a human for something they cannot get on their own — a permission escalation, an external action, a decision, missing input, or another blocker. It is the structured replacement for \"I gave up\" log lines and event errors. C(state=present) files a new human-todo via POST /api/human-todos. When C(parent_agent_todo_id) is set, the parent agent todo is moved to C(blocked_on_human) and will not be dispatched until the human resolves the request. C(state=done) marks an existing human-todo done (human provided what was asked). C(state=dismissed) records that the human declined, so the agent knows to try a different approach. Check mode skips write operations.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "present",
            "done",
            "dismissed"
          ],
          "default": "present"
        },
        "title": {
          "type": "string"
        },
        "body": {
          "type": "string"
        },
        "category": {
          "type": "string",
          "enum": [
            "permission_escalation",
            "external_action",
            "decision",
            "input_request",
            "blocker"
          ],
          "default": "blocker"
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high",
            "urgent"
          ],
          "default": "medium"
        },
        "parent_agent_todo_id": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "default": []
        },
        "id": {
          "type": "string"
        },
        "human_resolution": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "human_resolver": {
          "type": "string",
          "default": "operator"
        },
        "agent_id": {
          "type": "string",
          "default": "agent"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_introspect",
    "description": "Inject codebase self-knowledge facts (churn/complexity/coverage/debt) as ansible_facts — Queries the daemon's read-only C(GET /api/facts) endpoint and returns the C(codebase) self-introspection block under C(ansible_facts.gludd.codebase) so a self-improvement playbook can pick a high-value target (low coverage intersect high churn intersect debt). Read-only and check-mode safe — performs no writes. Exposes C(gludd.codebase.churn), C(gludd.codebase.complexity), C(gludd.codebase.coverage), C(gludd.codebase.debt), C(gludd.codebase.dead_code), C(gludd.codebase.missing_tests), C(gludd.codebase.perf_cost), and C(gludd.codebase.recent_failures). Each facet is C(null) when its source is unavailable — nothing is faked.",
    "input_schema": {
      "type": "object",
      "properties": {
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_langchain_generate",
    "description": "Generate text (optionally structured JSON) via the daemon — Sends a prompt to the daemon's POST /admin/models/call endpoint, which runs the generation through the LangChain-backed model gateway. When C(response_schema) is supplied the returned text is fence-stripped and parsed as JSON; a parse failure is returned as a clean module failure rather than a traceback. This module is stdlib-only; it never imports LangChain itself. All LangChain work happens server-side in the daemon.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string"
        },
        "system": {
          "type": "string",
          "default": ""
        },
        "model_profile": {
          "type": "string"
        },
        "route_task_type": {
          "type": "string"
        },
        "response_schema": {
          "type": "object"
        },
        "max_tokens": {
          "type": "integer",
          "default": 2048
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "prompt"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_langgraph_decision",
    "description": "Ask the model to choose one option from a fixed set — Sends a decision prompt plus a list of allowed option tokens to the daemon's POST /admin/models/call endpoint and asks the model to reply with JSON of the form {\"decision\": ..., \"rationale\": ...}. The reply is fence-stripped and JSON-parsed; the chosen decision is validated against C(options). On any failure (bad JSON, decision not in the option set) the module falls back to the first option, marks C(valid)=false, and records a warning rather than crashing. This module is stdlib-only; it never imports LangGraph/LangChain.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string"
        },
        "options": {
          "type": "array"
        },
        "system": {
          "type": "string"
        },
        "model_profile": {
          "type": "string"
        },
        "route_task_type": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "prompt",
        "options"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_langgraph_workflow",
    "description": "Run a multi-step LangGraph generate/review workflow — Sends a message list to the daemon's POST /admin/models/workflow endpoint, which executes a LangGraph generate -> review -> retry loop server-side and returns the best content plus quality metadata. This module is stdlib-only; it never imports LangGraph itself. All graph execution happens in the daemon.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string"
        },
        "system": {
          "type": "string",
          "default": ""
        },
        "work_type": {
          "type": "string",
          "default": "code"
        },
        "model_profile": {
          "type": "string",
          "default": "default"
        },
        "max_retries": {
          "type": "integer",
          "default": 2
        },
        "quality_threshold": {
          "type": "number",
          "default": 0.6
        },
        "enable_graph": {
          "type": "boolean",
          "default": true
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 300
        }
      },
      "additionalProperties": false,
      "required": [
        "prompt"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_make",
    "description": "Run a make target via the MakeRunner abstraction — Runs a C(make) target through the C(MakeRunner) subprocess wrapper with proper sanitized environment, bounded output capture, and per-target timeout. Supports both blocking and streaming modes. Returns structured result (rc, stdout_tail, stderr_tail, success, phases).",
    "input_schema": {
      "type": "object",
      "properties": {
        "target": {
          "type": "string"
        },
        "extra_args": {
          "type": "array",
          "default": []
        },
        "cwd": {
          "type": "string"
        },
        "timeout_s": {
          "type": "integer"
        },
        "env_extra": {
          "type": "object"
        },
        "stream": {
          "type": "boolean",
          "default": false
        },
        "no_log": {
          "type": "boolean",
          "default": false
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        }
      },
      "additionalProperties": false,
      "required": [
        "target"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_mcp_tool",
    "description": "Invoke an MCP tool (honest placeholder — not yet wired) — Per the W3.9 decision in TASKS.md (MCP honestly fenced): the daemon loads C(mcp_servers) config but passes C(mcp_client=None) — no MCP tools can be called through the daemon today. This module exists so playbooks can reference C(general_ludd.agent.gludd_mcp_tool) without import errors; it always returns C(not_implemented=true) and C(failed=false) so callers can C(when: not mcp_result.not_implemented) gate around it cleanly. When MCP wiring (W3.9 option a) is completed, replace the body of this module and remove this note.",
    "input_schema": {
      "type": "object",
      "properties": {
        "server": {
          "type": "string"
        },
        "tool": {
          "type": "string"
        },
        "arguments": {
          "type": "object",
          "default": {}
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false,
      "required": [
        "server",
        "tool"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_message",
    "description": "Inter-agent message queue — send, receive, or ack messages via the daemon — Talks to the daemon message-queue API so agents/roles can coordinate. C(state=send) posts a message to a recipient role/agent (or C(broadcast)). C(state=receive) fetches the inbox for C(recipient); messages are returned both as C(ansible_facts.gludd_inbox) and a C(messages) list. Pass C(ack=true) to mark every received message read in the same task. C(state=ack) marks a single C(message_id) read. Check mode skips the write side of C(send)/C(ack); C(receive) is always safe.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "send",
            "receive",
            "ack"
          ]
        },
        "sender": {
          "type": "string"
        },
        "recipient": {
          "type": "string"
        },
        "topic": {
          "type": "string",
          "default": ""
        },
        "body": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "normal",
            "high",
            "urgent"
          ],
          "default": "normal"
        },
        "ttl_seconds": {
          "type": "integer"
        },
        "message_id": {
          "type": "string"
        },
        "unread": {
          "type": "boolean",
          "default": true
        },
        "ack": {
          "type": "boolean",
          "default": false
        },
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false,
      "required": [
        "state"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_metrics",
    "description": "Inject live daemon metrics (agents/usage/cost/benchmarks) as ansible_facts — Queries the daemon's read-only C(GET /api/metrics) endpoint and returns the metrics snapshot under C(ansible_facts.gludd_metrics) so a playbook can branch on live cost/usage/benchmark data in C(when:) / C(vars:). Read-only and check-mode safe — it performs no writes. Exposes agent-level metrics, global per-model usage, per-project cost, and benchmark rankings (when benchmark data is available). Reuses the daemon's MetricsCollector / BenchmarkRepository — no stat logic is recomputed client-side.",
    "input_schema": {
      "type": "object",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "project_id": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_model_call",
    "description": "Run a model generation via the daemon API — Sends a prompt to the daemon's POST /admin/models/call endpoint. Supports direct model profile selection or adaptive routing by task type. Returns the generated text plus usage metadata.",
    "input_schema": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string"
        },
        "model_profile": {
          "type": "string"
        },
        "route_task_type": {
          "type": "string"
        },
        "max_tokens": {
          "type": "integer",
          "default": 2048
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "prompt"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_observe",
    "description": "Correlate registered observability sources through the Gludd daemon — Provides the Ansible seam for the cross-source C(GluddObserve) facade. Discovers only operator-registered sources from C(GET /api/observe/sources) and queries them by registered name through C(POST /api/observe/query). Callers cannot supply connector URLs, preserving the daemon's SSRF boundary. Supports merged queries, timelines, incident correlation, and service/host topology without duplicating the canonical normalization logic. Read-only and check-mode safe. A failing connector is isolated into the returned C(errors) list instead of aborting successful sources.",
    "input_schema": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "enum": [
            "query_sources",
            "timeline",
            "correlate_incident",
            "topology"
          ]
        },
        "role": {
          "type": "string",
          "default": "observe"
        },
        "seed": {
          "type": "object",
          "default": {}
        },
        "kinds": {
          "type": "array",
          "default": [
            "logs",
            "traces",
            "metrics",
            "events"
          ]
        },
        "by": {
          "type": "string",
          "default": "trace_id"
        },
        "window_s": {
          "type": "number",
          "default": 300.0
        },
        "spec": {
          "type": "object",
          "default": {}
        },
        "start": {
          "type": "number"
        },
        "end": {
          "type": "number"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false,
      "required": [
        "op"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_open_code",
    "description": "Batched opencode agent tool patterns — gate, push, commit, test, status — Codifies the repeated back-and-forth tool-call patterns opencode agents perform by bundling multiple tool calls into single Ansible tasks. Each action maps to a make target that composites the underlying checks. Check-mode safe — all actions report the change they WOULD make without executing the underlying command. All actions run locally via C(ansible.builtin.command) executing make targets in the repository root; no daemon round-trips are needed.",
    "input_schema": {
      "type": "object",
      "properties": {},
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_ornith",
    "description": "Pull rejected Ornith training pairs and invoke improvement rollouts — {'Bidirectional seam for the gludd x Ornith symbiotic loop. Two states': None} C(state=pairs) — fetch the most-recent training pairs whose outcome matches a comma-separated status list (e.g. C(rejected_by_gate,rejected_by_review,reverted)). These are the artifacts that NEED improvement. Hits the daemon's C(GET /admin/ornith/pairs) endpoint. C(state=improve) — invoke the daemon's model gateway (C(POST /admin/models/call)) with a structured \"improve this artifact\" prompt and return the proposed diff. The caller is responsible for writing the diff to disk, opening a PR, and filing a human-todo for review. The PR is NEVER auto-merged — the human-todo is the gate. Check mode skips both the network call and the model invocation.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "pairs",
            "improve"
          ],
          "default": "improve"
        },
        "status": {
          "type": "string",
          "default": "rejected_by_gate,rejected_by_review,reverted"
        },
        "limit": {
          "type": "integer",
          "default": 10
        },
        "lookback_days": {
          "type": "integer",
          "default": 14
        },
        "project_id": {
          "type": "string",
          "default": ""
        },
        "task_description": {
          "type": "string",
          "default": ""
        },
        "target_files": {
          "type": "array",
          "default": []
        },
        "max_iterations": {
          "type": "integer",
          "default": 5
        },
        "model_profile": {
          "type": "string",
          "default": ""
        },
        "agent_id": {
          "type": "string",
          "default": "ornith_self_improve"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_osquery",
    "description": "Query live system state via osquery and inject rows as ansible_facts — Runs a B(read-only) C(SELECT) query against C(osqueryi --json) and returns the result rows under C(ansible_facts.gludd_osquery) so a playbook (or the model running a job) can branch on real system state — processes, users, mounts, network interfaces, installed packages, system_info, etc. SECURITY — only C(SELECT) queries are permitted. The query is validated to start with C(SELECT) (after optional C(WITH ...) CTEs) and is rejected if it contains any mutating / side-effecting keyword (C(INSERT)/C(UPDATE)/C(DELETE)/C(DROP)/C(CREATE)/C(ALTER)/C(ATTACH)/ C(DETACH)/C(PRAGMA)/C(REPLACE)/C(VACUUM)). osquery's virtual tables are mostly read-only, but this module refuses to even hand a write-shaped query to the binary. The osquery binary is resolved from the daemon's filestore (C(binaries/osquery), downloaded on first use by the BinaryBootstrapper) when running in the same venv as the daemon; otherwise it falls back to an C(osqueryi) on the system C(PATH). Runs the binary via an explicit argument list (never C(shell=True)). Read-only and check-mode safe — it performs no writes. In check mode the query is validated and the binary located, but osquery is not executed.",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string"
        },
        "timeout": {
          "type": "integer",
          "default": 10
        },
        "osquery_path": {
          "type": "string",
          "default": ""
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        }
      },
      "additionalProperties": false,
      "required": [
        "query"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_ping",
    "description": "Verify daemon reachability — Pings the general_ludd daemon by calling /healthz. Returns C(pong=true) and C(daemon_reachable) flag. Safe to use in check mode (read-only).",
    "input_schema": {
      "type": "object",
      "properties": {
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 10
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_proc_monitor",
    "description": "Report resource utilization, I/O and locks for gludd-managed processes — Queries the daemon's managed-process stats API and returns per-process resource utilization under C(ansible_facts.gludd_proc_monitor) so a playbook (or the model running a job) can branch on real process health — CPU, memory (RSS/VMS), I/O counters, open file descriptors, thread count, context switches, open files and held locks. When C(pid) is C(0) (the default) the module enumerates every gludd-managed process via C(GET /admin/processes) and then fetches stats for each B(alive) process. A process that exits mid-scan (its per-pid stats call returns C(404)) is skipped rather than failing the whole task. When C(pid) is greater than C(0) only that single process's stats are returned. B(Read-only) and B(check-mode safe) — it performs no writes. In check mode no daemon call is made and an empty fact set is returned (mirroring C(gludd_osquery)). Talks to the daemon over HTTP via the shared C(GluddClient) (stdlib C(urllib) only — no third-party deps in the managed-node venv).",
    "input_schema": {
      "type": "object",
      "properties": {
        "pid": {
          "type": "integer",
          "default": 0
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 10
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_process",
    "description": "List, signal, and inspect daemon-managed processes — Talks to the general_ludd daemon's managed-process API so a playbook (or the model running a job) can enumerate the processes the daemon launched, inspect a live process's resource usage, or deliver a signal to one — all without ever calling C(kill)/C(ps) directly on the managed node. C(action=list) calls C(GET /admin/processes) and returns the registry of managed processes under C(ansible_facts.gludd_process). C(action=status) calls C(GET /admin/processes/{pid}/stats) and returns a live psutil snapshot for one process under C(ansible_facts.gludd_process.stats). C(action=signal) calls C(POST /admin/processes/{pid}/signal) to deliver a signal (optionally to the whole process group). Signal delivery is a state change, so it reports C(changed=True) on success. SECURITY — for C(action=signal) the signal name is validated client-side against a small allow-list before any request is sent (defence in depth; the daemon enforces the same set server-side). Disallowed signal names are rejected without contacting the daemon. Read-only and check-mode safe for C(list)/C(status). In check mode a C(signal) request is B(not) sent — the module reports the change it would make (C(changed=True)) instead.",
    "input_schema": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "list",
            "signal",
            "status"
          ]
        },
        "pid": {
          "type": "integer",
          "default": 0
        },
        "signal": {
          "type": "string",
          "default": "SIGTERM"
        },
        "group": {
          "type": "boolean",
          "default": false
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 10
        }
      },
      "additionalProperties": false,
      "required": [
        "action"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_push_guard",
    "description": "Enforce push-rate guard via force-push bypass tracking — Wraps the ForcePushTracker (scripts/push_rate_guard.py) as an idempotent Ansible module. Tracks consecutive GLUDD_FORCE_PUSH bypasses in a JSON state file and rejects further bypasses when the configured C(max_bypasses) threshold is exceeded within C(window_hours). Supports three states: C(check) to query whether a bypass is allowed, C(record) to persist a bypass event, and C(reset) to clear the counter (normal push). Check-mode safe — C(check) and C(record) report what would change without mutating the state file.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "check",
            "record",
            "reset"
          ],
          "default": "check"
        },
        "state_file": {
          "type": "string",
          "default": ".gate-logs/force-push-track.json"
        },
        "max_bypasses": {
          "type": "integer",
          "default": 5
        },
        "window_hours": {
          "type": "number",
          "default": 12.0
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_rag",
    "description": "Retrieval-Augmented Generation pipeline via module_utils.rag — With C(op=add_document) ingests text into an in-memory vector store backed by the L(rag module_utils,ansible_collections.general_ludd.agent.plugins.module_utils.rag). With C(op=query) embeds a question, retrieves the top-k closest chunks, builds a prompt with them as context, and asks the daemon model gateway for an answer. The pipeline uses the pluggable embedder from C(rag.py) (HashEmbedder by default, or a daemon-backed embedder when the daemon is reachable). Check-mode safe for C(op=query) — returns a placeholder answer.",
    "input_schema": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "enum": [
            "add_document",
            "query"
          ],
          "default": "query"
        },
        "text": {
          "type": "string"
        },
        "metadata": {
          "type": "object",
          "default": {}
        },
        "top_k": {
          "type": "integer",
          "default": 5
        },
        "chunk_size": {
          "type": "integer",
          "default": 1000
        },
        "chunk_overlap": {
          "type": "integer",
          "default": 200
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "text"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_reload",
    "description": "Hot-rotate a validated leaf code module with health-gated auto-rollback — Calls the authenticated Gludd daemon reload endpoint, which wraps C(general_ludd.reload.hot_reloader.HotReloader.reload_code_module): snapshots the live module bytes, C(os.replace)s the candidate source over the live path, C(importlib.reload)s the module, then runs a health gate (a C(/readyz) poll). If the health gate fails or the reload raises, the original bytes are restored and the module is reloaded again — the live module ends up exactly as it started. Fail-closed: a missing/non-importable target, a missing candidate, or a failed health gate all yield C(success=false). The collection validates and hashes the candidate but never imports the Gludd core runtime; daemon authentication is explicit.",
    "input_schema": {
      "type": "object",
      "properties": {
        "module_name": {
          "type": "string"
        },
        "candidate_source_path": {
          "type": "string"
        },
        "health_url": {
          "type": "string"
        },
        "health_timeout": {
          "type": "number",
          "default": 5.0
        },
        "config_dir": {
          "type": "string",
          "default": "config"
        },
        "base_source_path": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "expected_sha256": {
          "type": "string"
        },
        "idempotency_key": {
          "type": "string"
        },
        "result_path": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        }
      },
      "additionalProperties": false,
      "required": [
        "module_name",
        "candidate_source_path"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_schedule",
    "description": "Compute concurrency-safe execution batches via the daemon scheduler — Posts a list of work-item descriptors to C(POST /api/schedule) and returns the ordered concurrency-safe batches as C(ansible_facts.gludd_schedule). Each work item specifies its exclusive resource requirements, upstream dependencies, and whether it is greenfield (no shared-resource conflicts). The daemon scheduler topologically sorts items, then groups them into batches where items within a batch may run concurrently and all dependencies are satisfied by strictly earlier batches. PSK-authed; check-mode safe (skips the API call and returns empty batches).",
    "input_schema": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false,
      "required": [
        "items"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_skill",
    "description": "Select and render a skill with Jinja2 variables — Looks up a skill by name or trigger pattern and renders its body with Jinja2 C(StrictUndefined) — an unknown variable is an error, not silent empty text. Uses the shared C(render_skill) renderer also wired into C(execution.engine) so playbook and prompt paths render identically. Frontmatter C(required_vars) list is checked before rendering; missing vars fail the task with the variable name in the error message.",
    "input_schema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "trigger": {
          "type": "string"
        },
        "variables": {
          "type": "object",
          "default": {}
        },
        "skills_path": {
          "type": "string",
          "default": ""
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_slurm_deploy",
    "description": "Deploy a vLLM or llama.cpp model server on a Slurm cluster — Submits a Slurm batch job that launches C(vllm serve) or C(llama_cpp.server) on an allocated GPU node and polls until the server is servable (writes a servable.json artifact with servable_url). Wraps C(general_ludd.infra.slurm_deployment.VllmSlurmDeployment) and C(LlamacppSlurmDeployment). Use this when the operator has a Slurm cluster and Slurm should arbitrate GPU access (fairshare + accounting). For cloud GPU, use Terraform; for local dev, use C(make local-model-vllm).",
    "input_schema": {
      "type": "object",
      "properties": {
        "engine": {
          "type": "string",
          "enum": [
            "vllm",
            "llamacpp"
          ]
        },
        "model_id": {
          "type": "string"
        },
        "gpu_count": {
          "type": "integer",
          "default": 1
        },
        "gpu_type": {
          "type": "string",
          "default": "a100"
        },
        "port": {
          "type": "integer",
          "default": 8000
        },
        "max_hours": {
          "type": "integer",
          "default": 4
        },
        "mem_gb": {
          "type": "integer",
          "default": 32
        },
        "partition": {
          "type": "string",
          "default": "gpu"
        },
        "max_ctx": {
          "type": "integer",
          "default": 32768
        },
        "artifact_dir": {
          "type": "string"
        },
        "poll_timeout": {
          "type": "integer",
          "default": 300
        },
        "poll_interval": {
          "type": "number",
          "default": 5.0
        },
        "module_loads": {
          "type": "array",
          "default": []
        },
        "extra_args": {
          "type": "array",
          "default": []
        },
        "idempotency_key": {
          "type": "string"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        }
      },
      "additionalProperties": false,
      "required": [
        "engine",
        "model_id",
        "artifact_dir"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_spend",
    "description": "Fetch and configure the daemon spend-limiter — C(state=get) calls C(GET /api/spend) and returns the current spend snapshot as C(ansible_facts.gludd_spend). C(state=configure) calls C(POST /api/spend/configure) to update the spend-limiter settings (limit_usd and/or window_seconds). PSK-authed; check-mode safe on C(state=get). C(state=configure) skips the API call in check mode and returns an empty diff.",
    "input_schema": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "enum": [
            "get",
            "configure"
          ],
          "default": "get"
        },
        "limit_usd": {
          "type": "number"
        },
        "window_seconds": {
          "type": "integer"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_stream",
    "description": "Stream input signals (video/audio/text/binary) and dispatch buffered chunks to a cloned role — Opens a device (e.g. C(/dev/video0), C(hw:0,0), C(pulse), C(rtsp://...), a file path) via the appropriate capture tool (ffmpeg / tail / cat) and streams bytes into a rolling in-memory buffer. When the configured C(dispatch_trigger) fires (size threshold, interval, silence detection, or external MQ message), the current buffer is written to C(artifact_dir/chunk-<n>.bin) and POSTed to the daemon's C(/admin/stream/dispatch) endpoint. The dispatch clones the *calling role* (the role invoking this module), injects the chunk as a named variable, and runs the clone on that chunk. An optional C(external_processor) (whisper.cpp / ffmpeg / agent) is invoked on the chunk before the cloned role's tasks run. The module stops when the C(stop_condition) fires (timeout, EOF, external MQ message, or max dispatches), drains the remaining buffer with a final dispatch, closes the device subprocess, and returns. Check mode skips device capture and HTTP dispatch; it returns a synthetic result describing the would-have-run pipeline.",
    "input_schema": {
      "type": "object",
      "properties": {
        "device": {
          "type": "string"
        },
        "device_kind": {
          "type": "string",
          "enum": [
            "video",
            "audio",
            "text",
            "binary"
          ]
        },
        "buffer_size": {
          "type": "integer",
          "default": 1048576
        },
        "dispatch_trigger": {
          "type": "object"
        },
        "dispatch_role_clone": {
          "type": "object"
        },
        "external_processor": {
          "type": "object"
        },
        "stop_condition": {
          "type": "object"
        },
        "daemon_url": {
          "type": "string"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "artifact_dir": {
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "required": [
        "device",
        "device_kind",
        "daemon_url"
      ]
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_traces",
    "description": "Inject recent execution traces (spans/cost/phase) as ansible_facts — Queries the daemon's read-only C(GET /api/traces) endpoint and returns the recent execution-trace snapshot under C(ansible_facts.gludd_traces) so a playbook can branch on live trace data in C(when:) / C(vars:). Read-only and check-mode safe — it performs no writes. Exposes a bounded list of recent traces (trace_id, todo_id, work_type, total_cost_usd, total_tokens, success_rate, span_count, spans), a by-phase aggregate summary, and the OpenTelemetry exporter status (C(available) / C(disabled)). Traces are sourced ONLY from the daemon's in-process recent-traces buffer (genuinely-captured telemetry); no spans are fabricated. When no OTLP collector is configured the otel exporter status is honestly C(disabled).",
    "input_schema": {
      "type": "object",
      "properties": {
        "todo_id": {
          "type": "string"
        },
        "limit": {
          "type": "integer",
          "default": 20
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 30
        }
      },
      "additionalProperties": false
    },
    "server_id": "ansible"
  },
  {
    "name": "general_ludd.agent.gludd_worktree",
    "description": "Manage git worktrees (idempotent) — Creates or removes a git worktree via git_automation.repo.GitAutomation. Idempotent — C(changed=false) when the desired state already exists. In check mode reports what would change without modifying the filesystem.",
    "input_schema": {
      "type": "object",
      "properties": {
        "repo_path": {
          "type": "string"
        },
        "branch": {
          "type": "string"
        },
        "worktree_path": {
          "type": "string"
        },
        "state": {
          "type": "string",
          "enum": [
            "present",
            "absent"
          ],
          "default": "present"
        },
        "daemon_url": {
          "type": "string",
          "default": "http://localhost:8000"
        },
        "psk": {
          "type": "string",
          "default": "",
          "x-no-log": true
        },
        "timeout": {
          "type": "integer",
          "default": 120
        }
      },
      "additionalProperties": false,
      "required": [
        "repo_path",
        "branch",
        "worktree_path"
      ]
    },
    "server_id": "ansible"
  }
]
